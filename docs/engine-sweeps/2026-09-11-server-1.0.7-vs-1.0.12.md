<!-- generated by tools/diff-engine.js; edit the prose around it, not the tables -->

    from    : C:/Users/donfr/valheim-shadows/src/server-1.0.7
    to      : C:/Users/donfr/valheim-shadows/src/baseline-server
    surface : docs/ENGINE-SURFACE.md (278 members)

| verdict | count |
|---|---|
| unchanged | 273 |
| body changed | 4 |
| signature changed | 1 |
| gone | 0 |

## gone (0)

None.

## signature changed (1)

### Version

**`Version.c_networkVersion`** - 39 (m_networkVersion until 1.0)

    - public const uint c_networkVersion = 39u;
    + public const uint c_networkVersion = 40u;

```diff
--- C:/Users/donfr/valheim-shadows/src/server-1.0.7/assembly_valheim/Version.cs
+++ C:/Users/donfr/valheim-shadows/src/baseline-server/assembly_valheim/Version.cs
@@ -1,3 +1,3 @@
 
 
-	public const uint c_networkVersion = 39u;
+	public const uint c_networkVersion = 40u;
```

## body changed (4)

### Terminal

**`Terminal.ConsoleCommand`** - the constructor assigns into Terminal's map by lowered name, so re-registration is harmless

```diff
--- C:/Users/donfr/valheim-shadows/src/server-1.0.7/assembly_valheim/Terminal.cs
+++ C:/Users/donfr/valheim-shadows/src/baseline-server/assembly_valheim/Terminal.cs
@@ -130,15 +130,11 @@
 
 		public bool ShowCommand(Terminal context)
 		{
-			if (!IsSecret)
+			if (!IsSecret && (IsValid(context) || ((bool)ZNet.instance && !ZNet.instance.IsServer() && RemoteCommand)))
 			{
-				if (!IsValid(context))
+				if (!m_cheat)
 				{
-					if ((bool)ZNet.instance && !ZNet.instance.IsServer())
-					{
-						return RemoteCommand;
-					}
-					return false;
+					return !HideBehindDevCommands;
 				}
 				return true;
 			}
@@ -147,7 +143,7 @@
 
 		public bool IsValid(Terminal context, bool skipAllowedCheck = false)
 		{
-			if ((!IsCheat || context.IsCheatsEnabled()) && (!HideBehindDevCommands || context.IsCheatsEnabled()) && (context.isAllowedCommand(this) | skipAllowedCheck) && (!IsNetwork || (bool)ZNet.instance))
+			if ((!IsCheat || context.IsCheatsEnabled()) && (context.isAllowedCommand(this) | skipAllowedCheck) && (!IsNetwork || (bool)ZNet.instance))
 			{
 				if (OnlyServer)
 				{
```

**`Terminal.InitTerminal`** - our postfix registers the cargo console

```diff
--- C:/Users/donfr/valheim-shadows/src/server-1.0.7/assembly_valheim/Terminal.cs
+++ C:/Users/donfr/valheim-shadows/src/baseline-server/assembly_valheim/Terminal.cs
@@ -551,6 +551,29 @@
 				}
 			}
 		}, isCheat: false, isNetwork: false, onlyServer: true);
+		new ConsoleCommand("yesiuseddevcommandsbutiwantmyachievementsanyway", "[1 = true/ 0 = false] Normally cheated items / pieces don't trigger achievements. Turn this on to disable cheat checks.", delegate(ConsoleEventArgs args)
+		{
+			Player localPlayer = Player.m_localPlayer;
+			if (!(localPlayer == null))
+			{
+				string key = "bypasscheatchecks";
+				localPlayer.TryGetUniqueKeyValue(key, out var value);
+				int.TryParse(value, out var result);
+				if (!args.TryParameterInt(1, out var value2))
+				{
+					localPlayer.AddUniqueKeyValue(key, ((result != 1) ? 1 : 0).ToString());
+				}
+				else
+				{
+					localPlayer.AddUniqueKeyValue(key, (value2 == 1) ? "1" : "0");
+				}
+				if (localPlayer.TryGetUniqueKeyValue(key, out var value3))
+				{
+					string text = ((value3 == "1") ? "true" : "false");
+					args.Context.AddString("Set cheat bypass to " + text);
+				}
+			}
+		}, isCheat: false, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: true, hideBehindDevCommands: true);
 		new ConsoleCommand("getstat", "get specific stat", delegate(ConsoleEventArgs args)
 		{
 			if (args.Length < 2)
```

### ZNet

**`ZNet.ListContainsId`** - private; 1.0: the PlatformUserID.FilterPlatformUserID match (V_/X_/S_/N_/A_, Splatform.dll) OVERRIDES the bare and Steam_ forms; the lists want V_<steamid>

```diff
--- C:/Users/donfr/valheim-shadows/src/server-1.0.7/assembly_valheim/ZNet.cs
+++ C:/Users/donfr/valheim-shadows/src/baseline-server/assembly_valheim/ZNet.cs
@@ -11,7 +11,7 @@
 		PlatformUserID platformUserID = PlatformUserID.FilterPlatformUserID(platform);
 		if (platformUserID != platform)
 		{
-			flag = list.Contains(platformUserID.ToString());
+			flag |= list.Contains(platformUserID.ToString());
 		}
 		return flag;
 	}
```

**`ZNet.RPC_PeerInfo`** - ServerSync's buffering socket around the handshake

```diff
--- C:/Users/donfr/valheim-shadows/src/server-1.0.7/assembly_valheim/ZNet.cs
+++ C:/Users/donfr/valheim-shadows/src/baseline-server/assembly_valheim/ZNet.cs
@@ -16,8 +16,8 @@
 		}
 		string name = peer.m_socket.GetEndPointString();
 		string hostName = peer.m_socket.GetHostName();
-		ZLog.Log("Network version check, their:" + num + ", mine:" + 39u);
-		if (num != 39)
+		ZLog.Log("Network version check, their:" + num + ", mine:" + 40u);
+		if (num != 40)
 		{
 			if (m_isServer)
 			{
@@ -34,7 +34,7 @@
 				" has incompatible version, mine:",
 				Version.CurrentVersion.ToString(),
 				" (network version ",
-				39u.ToString(),
+				40u.ToString(),
 				")   remote ",
 				null,
 				null,
```

