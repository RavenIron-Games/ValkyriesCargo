# Valheim 0.221.12 -> 1.0.7, the client axis (2026-09-09)

**What this is.** `tools/diff-engine.js` over `docs/ENGINE-SURFACE.md` (257 rows), from the 0.221.12
baseline decompile (`~/valheim-shadows/src/baseline-client`, 2026-09-06) to the installed 1.0.7 client
as Steam updated it on 2026-09-09 (build 25185596; junction `~/valheim-shadows/client-1.0.7`, decompiled
with `tools/decompile-builds.sh --build client-1.0.7`, the P10a tools unmodified). The release, not the
playtest: `docs/engine-sweeps/2026-09-08-server-0.221.12-vs-0.221.13.md` was the hidden `public-test`
build, and what shipped differs from it by ~400 KB of assembly.

**The verdict, in one place.** The two playtest stop-ships EVAPORATED on the release: `GetStableHashCode`
is one-argument again (`assembly_utils`) and `ZNet.GetAllCharacterZDOS` has no early return. What the
release breaks in the 0.221.12 build of the mod, all six fixed on `a/valheim-1.0` (2026-09-10):
`Hoverable` gained `GetHoverOffset()` (CargoMerchant could not load); `ZoneSystem.m_activeArea` /
`m_activeDistantArea` are GONE and the active area is the synced `SimulationDistance` with a metre test
behind `ZNetScene.InActiveArea` (`Core/ActiveArea.cs`); `ZRoutedRpc.Everybody` became a `const`;
`MessageHud.ShowMessage`, the `ConsoleCommand` constructor and `EffectList.Create` each gained an
optional parameter; `Version.m_*` are `c_networkVersion` (39) / `c_PlayerVersion` (`Player.DeepNorth`,
46) / `c_WorldVersion` (`World.DeepNorth`, 41). `World.GetWorldSavePath` is gone and PR #55's resolver
already handles it (`save_path` PASSED). `RandEventSystem.SaveAsync` is gone; `Save` / `PrepareSave` /
`Load(BinaryReader, Version.World)` remain. Bodies read the same day, all holding: `SendCurrentRandomEvent`
(only the `Everybody` constant), `ZDO.IsValid` (`m_prefab != -1`, the window `Spawner.Author` closes),
`ZNetView.Awake` (Persistent still not re-applied), `ZDOMan.CreateNewZDO` (still no prefab set),
`Character.RPC_Damage` (the gate reordered; our prefix still runs first), `ZSyncTransform.OwnerSync` (the
velocity path byte-identical), `Valkyrie.UpdateValkyrie` (the maths unchanged), `Valkyrie.DropPlayer`
(+`WaitForRespawn`, a watch). The other 27 were read on 2026-09-10 and all hold: `docs/engine-sweeps/2026-09-10-1.0.7-bodies-read.md`.

The tables below are the tool's, verbatim.

---

<!-- generated by tools/diff-engine.js; edit the prose around it, not the tables -->

    from    : C:/Users/donfr/valheim-shadows/src/baseline-client
    to      : C:/Users/donfr/valheim-shadows/src/client-1.0.7
    surface : docs/ENGINE-SURFACE.md (257 members)

| verdict | count |
|---|---|
| unchanged | 206 |
| body changed | 35 |
| signature changed | 9 |
| gone | 7 |

## gone (7)

### RandEventSystem

**`RandEventSystem.SaveAsync`** - name, time and position

Present in `C:/Users/donfr/valheim-shadows/src/baseline-client`, absent in `C:/Users/donfr/valheim-shadows/src/client-1.0.7`.

```csharp
public void SaveAsync(BinaryWriter writer)
	{
		writer.Write(m_tempSaveEventTimer);
		writer.Write(m_tempSaveRandomEvent);
		writer.Write(m_tempSaveRandomEventTime);
		writer.Write(m_tempSaveRandomEventPos.x);
		writer.Write(m_tempSaveRandomEventPos.y);
		writer.Write(m_tempSaveRandomEventPos.z);
	}
```

### Version

**`Version.m_networkVersion`** - 36

Present in `C:/Users/donfr/valheim-shadows/src/baseline-client`, absent in `C:/Users/donfr/valheim-shadows/src/client-1.0.7`.

```csharp
public const uint m_networkVersion = 36u;
```

**`Version.m_playerVersion`** - 43

Present in `C:/Users/donfr/valheim-shadows/src/baseline-client`, absent in `C:/Users/donfr/valheim-shadows/src/client-1.0.7`.

```csharp
public const int m_playerVersion = 43;
```

**`Version.m_worldVersion`** - 37

Present in `C:/Users/donfr/valheim-shadows/src/baseline-client`, absent in `C:/Users/donfr/valheim-shadows/src/client-1.0.7`.

```csharp
public const int m_worldVersion = 37;
```

### World

**`World.GetWorldSavePath`** - MarketStore.Resolve; explicitly Local, because Auto/Cloud return "" under Steam Cloud

Present in `C:/Users/donfr/valheim-shadows/src/baseline-client`, absent in `C:/Users/donfr/valheim-shadows/src/client-1.0.7`.

```csharp
public static string GetWorldSavePath(FileHelpers.FileSource fileSource = FileHelpers.FileSource.Auto)
	{
		return Utils.GetSaveDataPath(fileSource) + ((fileSource == FileHelpers.FileSource.Local) ? "/worlds_local" : "/worlds");
	}
```

### ZoneSystem

**`ZoneSystem.m_activeArea`** - read LIVE, never the compiled default; the flight's block clamp

Present in `C:/Users/donfr/valheim-shadows/src/baseline-client`, absent in `C:/Users/donfr/valheim-shadows/src/client-1.0.7`.

```csharp
[Tooltip("Zones to load around center sector")]
	public int m_activeArea = 1;
```

**`ZoneSystem.m_activeDistantArea`** - cargo status

Present in `C:/Users/donfr/valheim-shadows/src/baseline-client`, absent in `C:/Users/donfr/valheim-shadows/src/client-1.0.7`.

```csharp
public int m_activeDistantArea = 1;
```

## signature changed (9)

### EffectList

**`EffectList.m_effectPrefabs`** - Patch_Terminal.DumpEffects

    - public EffectData[] m_effectPrefabs = new EffectData[0];
    + public EffectData[] m_effectPrefabs = Array.Empty<EffectData>();

```diff
--- C:/Users/donfr/valheim-shadows/src/baseline-client/assembly_valheim/EffectList.cs
+++ C:/Users/donfr/valheim-shadows/src/client-1.0.7/assembly_valheim/EffectList.cs
@@ -1,3 +1,3 @@
 
 
-	public EffectData[] m_effectPrefabs = new EffectData[0];
+	public EffectData[] m_effectPrefabs = Array.Empty<EffectData>();
```

### Inventory

**`Inventory.AddItem`** - DealApplier.AddStacks; caps ONE call at a stack

    - private bool AddItem(ItemDrop.ItemData item, int amount, int x, int y) ;; private bool AddItem(string name, int stack, float durability, Vector2i pos, bool equipped, int quality, int variant, long crafterID, string crafterName, Dictionary<string, string> customData, int worldLevel, bool pickedUp) ;; public ItemDrop.ItemData AddItem(string name, int stack, int quality, int variant, long crafterID, string crafterName, Vector2i position, bool pickedUp = false) ;; public ItemDrop.ItemData AddItem(string name, int stack, int quality, int variant, long crafterID, string crafterName, bool pickedUp = false) ;; public bool AddItem(GameObject prefab, int amount) ;; public bool AddItem(ItemDrop.ItemData item) ;; public bool AddItem(ItemDrop.ItemData item, Vector2i pos)
    + private bool AddItem(ItemDrop.ItemData item, int amount, int x, int y, bool skipValidPositionCheck = false) ;; private bool AddItem(int prefabHash, ItemDrop.ItemData itemData, bool skipValidPositionCheck = true) ;; private bool AddItem(int prefabHash, int stack, float durability, Vector2i pos, bool equipped, int quality, int variant, long crafterID, string crafterName, Dictionary<string, string> customData, int worldLevel, bool pickedUp, bool cheated, bool skipValidPositionCheck = false) ;; private bool AddItem(string name, int stack, float durability, Vector2i pos, bool equipped, int quality, int variant, long crafterID, string crafterName, Dictionary<string, string> customData, int worldLevel, bool pickedUp, bool cheated, bool skipValidPositionCheck = false) ;; public ItemDrop.ItemData AddItem(string name, int stack, int quality, int variant, long crafterID, string crafterName, Vector2i position, bool cheated, bool pickedUp = false, bool dropIfFullInv = true) ;; public ItemDrop.ItemData AddItem(string name, int stack, int quality, int variant, long crafterID, string crafterName, bool cheated, bool pickedUp = false) ;; public bool AddItem(GameObject prefab, int amount) ;; public bool AddItem(ItemDrop.ItemData item) ;; public bool AddItem(ItemDrop.ItemData item, Vector2i pos)

```diff
--- C:/Users/donfr/valheim-shadows/src/baseline-client/assembly_valheim/Inventory.cs
+++ C:/Users/donfr/valheim-shadows/src/client-1.0.7/assembly_valheim/Inventory.cs
@@ -1,17 +1,18 @@
 
 
-	private bool AddItem(ItemDrop.ItemData item, int amount, int x, int y)
+	private bool AddItem(ItemDrop.ItemData item, int amount, int x, int y, bool skipValidPositionCheck = false)
 	{
 		amount = Mathf.Min(amount, item.m_stack);
-		if (x < 0 || y < 0 || x >= m_width || y >= m_height)
+		if ((x < 0 || y < 0 || x >= m_width || (y >= m_height && !skipValidPositionCheck)) && !m_temoraryInventory)
 		{
 			return false;
 		}
 		bool flag = false;
+		bool cheatedStateChanged = item.m_cheated && !Achievements.IsCheatedAtAll() && Player.m_localPlayer?.GetInventory() == this;
 		ItemDrop.ItemData itemAt = GetItemAt(x, y);
 		if (itemAt != null)
 		{
-			if (itemAt.m_shared.m_name != item.m_shared.m_name || itemAt.m_worldLevel != item.m_worldLevel || (itemAt.m_shared.m_maxQuality > 1 && itemAt.m_quality != item.m_quality))
+			if (!itemAt.IsSameType(item))
 			{
 				return false;
 			}
@@ -35,17 +36,30 @@
 			item.m_stack -= amount;
 			flag = true;
 		}
-		Changed();
+		if (Player.m_localPlayerExists)
+		{
+			Changed(success: true, cheatedStateChanged);
+		}
 		return flag;
 	}
 
 
-	private bool AddItem(string name, int stack, float durability, Vector2i pos, bool equipped, int quality, int variant, long crafterID, string crafterName, Dictionary<string, string> customData, int worldLevel, bool pickedUp)
+	private bool AddItem(int prefabHash, ItemDrop.ItemData itemData, bool skipValidPositionCheck = true)
 	{
-		GameObject itemPrefab = ObjectDB.instance.GetItemPrefab(name);
+		return AddItem(prefabHash, itemData.m_stack, itemData.m_durability, itemData.m_gridPos, itemData.m_equipped, itemData.m_quality, itemData.m_variant, itemData.m_crafterID, itemData.m_crafterName, itemData.m_customData, itemData.m_worldLevel, itemData.m_pickedUp, itemData.m_cheated, skipValidPositionCheck);
+	}
+
+
+	private bool AddItem(int prefabHash, int stack, float durability, Vector2i pos, bool equipped, int quality, int variant, long crafterID, string crafterName, Dictionary<string, string> customData, int worldLevel, bool pickedUp, bool cheated, bool skipValidPositionCheck = false)
+	{
+		if (m_temoraryInventory)
+		{
+			return AddTempItem(prefabHash, stack, durability, pos, equipped, quality, variant, crafterID, crafterName, customData, worldLevel, pickedUp);
+		}
+		GameObject itemPrefab = ObjectDB.instance.GetItemPrefab(prefabHash);
 		if (itemPrefab == null)
 		{
-			ZLog.Log("Failed to find item prefab " + name);
+			ZLog.Log("Failed to find item prefab " + prefabHash);
 			return false;
 		}
 		ZNetView.m_forceDisableInit = true;
@@ -54,7 +68,7 @@
 		ItemDrop component = gameObject.GetComponent<ItemDrop>();
 		if (component == null)
 		{
-			ZLog.Log("Missing itemdrop in " + name);
+			ZLog.Log("Missing itemdrop in " + prefabHash);
 			UnityEngine.Object.Destroy(gameObject);
 			return false;
 		}
@@ -68,14 +82,22 @@
 		component.m_itemData.m_customData = customData;
 		component.m_itemData.m_worldLevel = (byte)worldLevel;
 		component.m_itemData.m_pickedUp = pickedUp;
-		AddItem(component.m_itemData, component.m_itemData.m_stack, pos.x, pos.y);
+		component.m_itemData.m_cheated = cheated || component.m_itemData.GetDamage().GetTotalDamage() > 10000f;
+		AddItem(component.m_itemData, component.m_itemData.m_stack, pos.x, pos.y, skipValidPositionCheck);
 		UnityEngine.Object.Destroy(gameObject);
 		return true;
 	}
 
 
-	public ItemDrop.ItemData AddItem(string name, int stack, int quality, int variant, long crafterID, string crafterName, Vector2i position, bool pickedUp = false)
+	private bool AddItem(string name, int stack, float durability, Vector2i pos, bool equipped, int quality, int variant, long crafterID, string crafterName, Dictionary<string, string> customData, int worldLevel, bool pickedUp, bool cheated, bool skipValidPositionCheck = false)
 	{
+		int prefabHash = ((!string.IsNullOrEmpty(name)) ? name.GetStableHashCode() : 0);
+		return AddItem(prefabHash, stack, durability, pos, equipped, quality, variant, crafterID, crafterName, customData, worldLevel, pickedUp, cheated, skipValidPositionCheck);
+	}
+
+
+	public ItemDrop.ItemData AddItem(string name, int stack, int quality, int variant, long crafterID, string crafterName, Vector2i position, bool cheated, bool pickedUp = false, bool dropIfFullInv = true)
+	{
 		GameObject itemPrefab = ObjectDB.instance.GetItemPrefab(name);
 		if (itemPrefab == null)
 		{
@@ -116,10 +138,17 @@
 			component2.m_itemData.m_crafterName = crafterName;
 			component2.m_itemData.m_worldLevel = (byte)Game.m_worldLevel;
 			component2.m_itemData.m_pickedUp = pickedUp;
+			component2.m_itemData.m_cheated = cheated;
 			if (!((position.x >= 0 && position.y >= 0 && position.x < m_width && position.y < m_height) ? AddItem(component2.m_itemData, position) : AddItem(component2.m_itemData)))
 			{
-				UnityEngine.Object.Destroy(gameObject);
-				return null;
+				if (!dropIfFullInv)
+				{
+					UnityEngine.Object.Destroy(gameObject);
+					ZLog.LogError("Failed to add new item " + name + " in inventory, deleting!");
+					return null;
+				}
+				UnityEngine.Object.Instantiate(itemPrefab, Player.m_localPlayer.transform.position + Vector3.up, Quaternion.identity);
+				ZLog.LogWarning("Failed to add new item " + name + " in inventory, dropping!");
 			}
 			result = component2.m_itemData;
 			UnityEngine.Object.Destroy(gameObject);
@@ -128,9 +157,9 @@
 	}
 
 
-	public ItemDrop.ItemData AddItem(string name, int stack, int quality, int variant, long crafterID, string crafterName, bool pickedUp = false)
+	public ItemDrop.ItemData AddItem(string name, int stack, int quality, int variant, long crafterID, string crafterName, bool cheated, bool pickedUp = false)
 	{
-		return AddItem(name, stack, quality, variant, crafterID, crafterName, new Vector2i(-1, -1), pickedUp);
+		return AddItem(name, stack, quality, variant, crafterID, crafterName, new Vector2i(-1, -1), cheated, pickedUp);
 	}
 
 
@@ -140,14 +169,15 @@
 		itemData.m_dropPrefab = prefab;
 		itemData.m_stack = Mathf.Min(amount, itemData.m_shared.m_maxStackSize);
 		itemData.m_worldLevel = (byte)Game.m_worldLevel;
-		ZLog.Log("adding " + prefab.name + "  " + itemData.m_stack);
+		ZLog.Log($"Adding item {prefab.name} x{itemData.m_stack}");
 		return AddItem(itemData);
 	}
 
 
 	public bool AddItem(ItemDrop.ItemData item)
 	{
-		bool result = true;
+		bool flag = true;
+		bool cheatedStateChanged = item.m_cheated && !Achievements.IsCheatedAtAll();
 		if (item.m_shared.m_maxStackSize > 1)
 		{
 			for (int i = 0; i < item.m_stack; i++)
@@ -156,6 +186,10 @@
 				if (itemData != null)
 				{
 					itemData.m_stack++;
+					if (item.m_cheated && !PlayerProfile.s_bypassCheatChecks)
+					{
+						itemData.m_cheated = true;
+					}
 					continue;
 				}
 				int stack = item.m_stack - i;
@@ -168,7 +202,8 @@
 				}
 				else
 				{
-					result = false;
+					flag = false;
+					ZLog.LogError($"Trying to add item to occupied slot {gridPos.x}, {gridPos.y}");
 				}
 				break;
 			}
@@ -183,17 +218,19 @@
 			}
 			else
 			{
-				result = false;
+				flag = false;
+				ZLog.LogError($"Trying to add item to occupied slot {gridPos2.x}, {gridPos2.y}");
 			}
 		}
-		Changed();
-		return result;
+		Changed(flag, cheatedStateChanged);
+		return flag;
 	}
 
 
 	public bool AddItem(ItemDrop.ItemData item, Vector2i pos)
 	{
-		bool result = true;
+		bool flag = true;
+		bool cheatedStateChanged = item.m_cheated && !Achievements.IsCheatedAtAll();
 		if (item.m_shared.m_maxStackSize > 1)
 		{
 			for (int i = 0; i < item.m_stack; i++)
@@ -213,7 +250,8 @@
 				}
 				else
 				{
-					result = false;
+					flag = false;
+					ZLog.LogError($"Trying to add item to occupied slot {pos.x}, {pos.y}");
 				}
 				break;
 			}
@@ -225,8 +263,9 @@
 		}
 		else
 		{
-			result = false;
+			flag = false;
+			ZLog.LogError($"Trying to add item to occupied slot {pos.x}, {pos.y}");
 		}
-		Changed();
-		return result;
+		Changed(flag, cheatedStateChanged);
+		return flag;
 	}
```

### MessageHud

**`MessageHud.ShowMessage`** - CargoTick.PilotLine, Deliveries.Announce

    - public void ShowMessage(MessageType type, string text, int amount = 0, Sprite icon = null, bool showDespiteHiddenHUD = false)
    + public void ShowMessage(MessageType type, string text, int amount = 0, Sprite icon = null, bool showDespiteHiddenHUD = false, bool log = true)

```diff
--- C:/Users/donfr/valheim-shadows/src/baseline-client/assembly_valheim/MessageHud.cs
+++ C:/Users/donfr/valheim-shadows/src/client-1.0.7/assembly_valheim/MessageHud.cs
@@ -1,38 +1,46 @@
 
 
-	public void ShowMessage(MessageType type, string text, int amount = 0, Sprite icon = null, bool showDespiteHiddenHUD = false)
+	public void ShowMessage(MessageType type, string text, int amount = 0, Sprite icon = null, bool showDespiteHiddenHUD = false, bool log = true)
 	{
 		m_showDespiteHiddenHUD = showDespiteHiddenHUD;
-		if (!Hud.IsUserHidden() || showDespiteHiddenHUD)
+		if (Hud.IsUserHidden() && !showDespiteHiddenHUD)
 		{
-			text = Localization.instance.Localize(text);
-			switch (type)
-			{
-			case MessageType.TopLeft:
+			return;
+		}
+		text = Localization.instance.Localize(text);
+		switch (type)
+		{
+		case MessageType.TopLeft:
+		{
+			MsgData msgData = new MsgData();
+			msgData.m_icon = icon;
+			msgData.m_text = text;
+			msgData.m_amount = amount;
+			m_msgQeue.Enqueue(msgData);
+			if (log)
 			{
-				MsgData msgData = new MsgData();
-				msgData.m_icon = icon;
-				msgData.m_text = text;
-				msgData.m_amount = amount;
-				m_msgQeue.Enqueue(msgData);
 				AddLog(text);
-				break;
 			}
-			case MessageType.Center:
-				m_messageCenterText.text = text;
-				_crossFadeTextBuffer.Add(new CrossFadeText
-				{
-					text = m_messageCenterText,
-					alpha = 1f,
-					time = 0f
-				});
-				_crossFadeTextBuffer.Add(new CrossFadeText
-				{
-					text = m_messageCenterText,
-					alpha = 0f,
-					time = 4f
-				});
-				break;
+			break;
+		}
+		case MessageType.Center:
+			m_messageCenterText.text = text;
+			_crossFadeTextBuffer.Add(new CrossFadeText
+			{
+				text = m_messageCenterText,
+				alpha = 1f,
+				time = 0f
+			});
+			_crossFadeTextBuffer.Add(new CrossFadeText
+			{
+				text = m_messageCenterText,
+				alpha = 0f,
+				time = 4f
+			});
+			if (log)
+			{
+				AddLog(text);
 			}
+			break;
 		}
 	}
```

### RandEventSystem

**`RandEventSystem.Load`** - restores through SetRandomEventByName; the director adopts it within 15 s

    - public void Load(BinaryReader reader, int version)
    + public void Load(BinaryReader reader, Version.World version)

```diff
--- C:/Users/donfr/valheim-shadows/src/baseline-client/assembly_valheim/RandEventSystem.cs
+++ C:/Users/donfr/valheim-shadows/src/client-1.0.7/assembly_valheim/RandEventSystem.cs
@@ -1,9 +1,9 @@
 
 
-	public void Load(BinaryReader reader, int version)
+	public void Load(BinaryReader reader, Version.World version)
 	{
 		m_eventTimer = reader.ReadSingle();
-		if (version < 25)
+		if (version < Version.World.SupportRandomEvent2)
 		{
 			return;
 		}
```

### SEMan

**`SEMan.HaveStatusEffect`** - ComfortReporter; the int overload

    - public bool HaveStatusEffect(int nameHash)
    + public bool HaveStatusEffect(StatusEffect se) ;; public bool HaveStatusEffect(int nameHash)

```diff
--- C:/Users/donfr/valheim-shadows/src/baseline-client/assembly_valheim/SEMan.cs
+++ C:/Users/donfr/valheim-shadows/src/client-1.0.7/assembly_valheim/SEMan.cs
@@ -1,6 +1,12 @@
 
 
+	public bool HaveStatusEffect(StatusEffect se)
+	{
+		return GetStatusEffect(se.NameHash());
+	}
+
+
 	public bool HaveStatusEffect(int nameHash)
 	{
 		return m_statusEffectsHashSet.Contains(nameHash);
 	}
```

### ZNetScene

**`ZNetScene.InActiveArea`** -  | zone - centre | <= activeArea - 1; reimplemented in Core/FlightPlan.cs

    - public static bool InActiveArea(Vector2i zone, Vector2i refCenterZone) ;; public static bool InActiveArea(Vector2i zone, Vector2i refCenterZone, int activatedArea) ;; public static bool InActiveArea(Vector2i zone, Vector3 refPoint)
    + public static bool InActiveArea(Vector3 point, Vector2s centerZone) ;; public static bool InActiveArea(Vector3 point, Vector3 centerPosition)

```diff
--- C:/Users/donfr/valheim-shadows/src/baseline-client/assembly_valheim/ZNetScene.cs
+++ C:/Users/donfr/valheim-shadows/src/client-1.0.7/assembly_valheim/ZNetScene.cs
@@ -1,28 +1,12 @@
 
 
-	public static bool InActiveArea(Vector2i zone, Vector2i refCenterZone)
-	{
-		int num = ZoneSystem.instance.m_activeArea - 1;
-		if (zone.x >= refCenterZone.x - num && zone.x <= refCenterZone.x + num && zone.y <= refCenterZone.y + num)
-		{
-			return zone.y >= refCenterZone.y - num;
-		}
-		return false;
-	}
-
-
-	public static bool InActiveArea(Vector2i zone, Vector2i refCenterZone, int activatedArea)
+	public static bool InActiveArea(Vector3 point, Vector2s centerZone)
 	{
-		if (zone.x >= refCenterZone.x - activatedArea && zone.x <= refCenterZone.x + activatedArea && zone.y <= refCenterZone.y + activatedArea)
-		{
-			return zone.y >= refCenterZone.y - activatedArea;
-		}
-		return false;
+		return PointInsideActiveArea(centerZone, point);
 	}
 
 
-	public static bool InActiveArea(Vector2i zone, Vector3 refPoint)
+	public static bool InActiveArea(Vector3 point, Vector3 centerPosition)
 	{
-		Vector2i zone2 = ZoneSystem.GetZone(refPoint);
-		return InActiveArea(zone, zone2);
+		return InActiveArea(point, ZoneSystem.GetZone(centerPosition));
 	}
```

### ZRoutedRpc

**`ZRoutedRpc.Everybody`** - ServerSync

    - public static long Everybody;
    + public const long Everybody = 0L;

```diff
--- C:/Users/donfr/valheim-shadows/src/baseline-client/assembly_valheim/ZRoutedRpc.cs
+++ C:/Users/donfr/valheim-shadows/src/client-1.0.7/assembly_valheim/ZRoutedRpc.cs
@@ -1,3 +1,3 @@
 
 
-	public static long Everybody;
+	public const long Everybody = 0L;
```

### ZoneSystem

**`ZoneSystem.GetZone`** - floor((v + zoneSize/2) / zoneSize); reimplemented in Core/FlightPlan.cs

    - public static Vector2i GetZone(Vector3 point)
    + public static Vector2s GetZone(Vector3 point)

```diff
--- C:/Users/donfr/valheim-shadows/src/baseline-client/assembly_valheim/ZoneSystem.cs
+++ C:/Users/donfr/valheim-shadows/src/client-1.0.7/assembly_valheim/ZoneSystem.cs
@@ -1,8 +1,8 @@
 
 
-	public static Vector2i GetZone(Vector3 point)
+	public static Vector2s GetZone(Vector3 point)
 	{
 		int x = Utils.FloorToInt((float)(((double)point.x + 32.0) / 64.0));
 		int y = Utils.FloorToInt((float)(((double)point.z + 32.0) / 64.0));
-		return new Vector2i(x, y);
+		return new Vector2s(x, y);
 	}
```

**`ZoneSystem.instance`** - Spawner, CargoFlight, Patch_Terminal

    - public static ZoneSystem instance => m_instance;
    + public static ZoneSystem instance => s_instance;

```diff
--- C:/Users/donfr/valheim-shadows/src/baseline-client/assembly_valheim/ZoneSystem.cs
+++ C:/Users/donfr/valheim-shadows/src/client-1.0.7/assembly_valheim/ZoneSystem.cs
@@ -1,3 +1,3 @@
 
 
-	public static ZoneSystem instance => m_instance;
+	public static ZoneSystem instance => s_instance;
```

## body changed (35)

### Character

**`Character.Awake`** - caches m_animator = GetComponentInChildren<Animator>(); BodyLoader's appended-last defence

```diff
--- C:/Users/donfr/valheim-shadows/src/baseline-client/assembly_valheim/Character.cs
+++ C:/Users/donfr/valheim-shadows/src/client-1.0.7/assembly_valheim/Character.cs
@@ -26,6 +26,8 @@
 			s_groundRayMask = LayerMask.GetMask("Default", "static_solid", "Default_small", "piece", "terrain", "blocker", "vehicle");
 			s_characterLayerMask = LayerMask.GetMask("character", "character_noenv");
 			s_blockedRayMask = LayerMask.GetMask("piece", "Default", "static_solid", "Default_small", "terrain");
+			s_slipperyStuffMask = LayerMask.GetMask("static_solid", "Default");
+			m_snowPieceRayMask = LayerMask.GetMask("piece", "piece_nonsolid", "item", "terrain");
 		}
 		if ((bool)m_lodGroup)
 		{
@@ -52,6 +54,15 @@
 			m_nview.Register<bool>("RPC_SetTamed", RPC_SetTamed);
 			m_nview.Register<float>("RPC_FreezeFrame", RPC_FreezeFrame);
 			m_nview.Register<Vector3, Quaternion, bool>("RPC_TeleportTo", RPC_TeleportTo);
+		}
+		m_lastHealth = GetHealth();
+		if (m_nview.IsOwner())
+		{
+			float maxHealth = GetMaxHealth();
+			if (m_lastHealth.Equals(maxHealth))
+			{
+				m_nview.GetZDO().RemoveFloat(ZDOVars.s_health);
+			}
 		}
 		if (!IsPlayer())
 		{
```

**`Character.Faction`** - nested public enum, Players first; what CargoMerchant.Reassert sets so vanilla treats him as a player ally for aggro and targeting

```diff
--- C:/Users/donfr/valheim-shadows/src/baseline-client/assembly_valheim/Character.cs
+++ C:/Users/donfr/valheim-shadows/src/client-1.0.7/assembly_valheim/Character.cs
@@ -13,5 +13,6 @@
 		MistlandsMonsters,
 		Dverger,
 		PlayerSpawned,
-		TrainingDummy
+		TrainingDummy,
+		DeepNorth
 	}
```

**`Character.RPC_Damage`** - private; our prefix (Patch_Character_Damage, Priority.Low, __runOriginal) cancels it for the merchant HERE, not on Damage - the victim-side choke point every hit passes through

```diff
--- C:/Users/donfr/valheim-shadows/src/baseline-client/assembly_valheim/Character.cs
+++ C:/Users/donfr/valheim-shadows/src/client-1.0.7/assembly_valheim/Character.cs
@@ -15,21 +15,81 @@
 		{
 			return;
 		}
+		if (Terminal.m_showTests && Terminal.m_testList.ContainsKey("damage"))
+		{
+			Terminal.Log("RPC_Damage: " + hit);
+		}
 		Character attacker = hit.GetAttacker();
 		if (!IsDead() && !InCutscene() && !IsTeleporting() && hit.m_staggerMultiplier >= 100f)
 		{
 			Stagger(hit.m_dir);
 		}
-		if (GetHealth() <= 0f || IsDead() || IsTeleporting() || InCutscene() || (hit.m_dodgeable && IsDodgeInvincible()) || (hit.HaveAttacker() && attacker == null) || (IsPlayer() && !IsPVPEnabled() && attacker != null && attacker.IsPlayer() && !hit.m_ignorePVP))
+		if (GetHealth() <= 0f || IsDead() || IsTeleporting() || InCutscene() || (hit.m_dodgeable && IsDodgeInvincible()))
 		{
 			return;
 		}
+		if (hit.m_eitrAdd > 0f)
+		{
+			AddEitr(hit.m_eitrAdd);
+		}
+		if ((hit.HaveAttacker() && attacker == null) || (IsPlayer() && !IsPVPEnabled() && attacker != null && attacker.IsPlayer() && !hit.m_ignorePVP))
+		{
+			return;
+		}
 		if (attacker != null && !attacker.IsPlayer())
 		{
 			float difficultyDamageScalePlayer = Game.instance.GetDifficultyDamageScalePlayer(base.transform.position);
 			hit.ApplyModifier(difficultyDamageScalePlayer);
 			hit.ApplyModifier(Game.m_enemyDamageRate);
 		}
+		if (attacker is Player player)
+		{
+			int s_attackers = ZDOVars.s_attackers;
+			string text = s_attackers + player.GetPlayerName();
+			if (!m_nview.GetZDO().GetBool(text))
+			{
+				m_nview.GetZDO().Set(text, value: true);
+				int num = m_nview.GetZDO().GetInt(ZDOVars.s_attackers);
+				m_nview.GetZDO().Set(ZDOVars.s_attackers, num + 1);
+			}
+		}
+		if (!IsPlayer())
+		{
+			KillModifiers killModifiers = (KillModifiers)m_nview.GetZDO().GetInt(ZDOVars.s_modifiers, 5);
+			if (killModifiers != KillModifiers.MixedAndTotal)
+			{
+				KillModifiers killModifiers2 = killModifiers;
+				switch (hit.m_skill)
+				{
+				case Skills.SkillType.Swords:
+				case Skills.SkillType.Knives:
+				case Skills.SkillType.Clubs:
+				case Skills.SkillType.Polearms:
+				case Skills.SkillType.Spears:
+				case Skills.SkillType.Blocking:
+				case Skills.SkillType.Axes:
+				case Skills.SkillType.Pickaxes:
+				case Skills.SkillType.WoodCutting:
+					killModifiers2 = KillModifiers.Melee;
+					break;
+				case Skills.SkillType.Bows:
+				case Skills.SkillType.Crossbows:
+					killModifiers2 = KillModifiers.Ranged;
+					break;
+				case Skills.SkillType.ElementalMagic:
+				case Skills.SkillType.BloodMagic:
+					killModifiers2 = KillModifiers.Magic;
+					break;
+				case Skills.SkillType.Unarmed:
+					killModifiers2 = ((hit.m_statusEffectHash == 99999) ? KillModifiers.Unarmed : KillModifiers.Melee);
+					break;
+				}
+				if (killModifiers != killModifiers2)
+				{
+					m_nview.GetZDO().Set(ZDOVars.s_modifiers, (int)((killModifiers == KillModifiers.CountNone) ? killModifiers2 : KillModifiers.MixedAndTotal));
+				}
+			}
+		}
 		m_seman.OnDamaged(hit, attacker);
 		if (m_baseAI != null && m_baseAI.IsAggravatable() && !m_baseAI.IsAggravated() && (bool)attacker && attacker.IsPlayer() && hit.GetTotalDamage() > 0f)
 		{
@@ -39,28 +99,28 @@
 		{
 			m_backstabTime = Time.time;
 			hit.ApplyModifier(hit.m_backstabBonus);
-			m_backstabHitEffects.Create(hit.m_point, Quaternion.identity, base.transform);
+			m_backstabHitEffects.Create(hit.m_point, Quaternion.identity, base.transform, 1f, -1, attacker?.GetZDOID() ?? ZDOID.None);
 		}
 		if (IsStaggering() && !IsPlayer())
 		{
 			hit.ApplyModifier(2f);
-			m_critHitEffects.Create(hit.m_point, Quaternion.identity, base.transform);
+			m_critHitEffects.Create(hit.m_point, Quaternion.identity, base.transform, 1f, -1, attacker?.GetZDOID() ?? ZDOID.None);
 		}
 		if (hit.m_blockable && IsBlocking())
 		{
 			BlockAttack(hit, attacker);
 		}
-		else if (this is Player player)
+		else if (this is Player player2)
 		{
-			AddAdrenaline(player.m_nonBlockDamageAdrenaline);
+			AddAdrenaline(player2.m_nonBlockDamageAdrenaline);
 		}
 		ApplyPushback(hit);
-		if (hit.m_statusEffectHash != 0)
+		if (hit.m_statusEffectHash != 0 && (hit.m_statusEffectHash != SEMan.s_statusEffectBurning || m_damageModifiers.m_fire != HitData.DamageModifier.Immune) && (hit.m_statusEffectHash != SEMan.s_statusEffectFrost || m_damageModifiers.m_frost != HitData.DamageModifier.Immune) && (hit.m_statusEffectHash != SEMan.s_statusEffectLightning || m_damageModifiers.m_lightning != HitData.DamageModifier.Immune) && (hit.m_statusEffectHash != SEMan.s_statusEffectPoison || m_damageModifiers.m_poison != HitData.DamageModifier.Immune))
 		{
 			StatusEffect statusEffect = m_seman.GetStatusEffect(hit.m_statusEffectHash);
 			if (statusEffect == null)
 			{
-				statusEffect = m_seman.AddStatusEffect(hit.m_statusEffectHash, resetTime: false, hit.m_itemLevel, hit.m_skillLevel);
+				statusEffect = m_seman.AddStatusEffect(hit.m_statusEffectHash, resetTime: false, hit.m_itemLevel, hit.m_skillLevel, hit.m_variant);
 			}
 			else
 			{
@@ -96,9 +156,9 @@
 		hit.m_damage.m_fire = 0f;
 		hit.m_damage.m_spirit = 0f;
 		ApplyDamage(hit, showDamageText: true, triggerEffects: true, significantModifier);
-		AddFireDamage(fire);
-		AddSpiritDamage(spirit);
-		AddPoisonDamage(poison);
-		AddFrostDamage(hit.m_damage.m_frost);
-		AddLightningDamage(hit.m_damage.m_lightning);
+		AddFireDamage(fire, hit.m_variant);
+		AddSpiritDamage(spirit, hit.m_variant);
+		AddPoisonDamage(poison, hit.m_variant);
+		AddFrostDamage(hit.m_damage.m_frost, hit.m_variant);
+		AddLightningDamage(hit.m_damage.m_lightning, hit.m_variant);
 	}
```

**`Character.m_name`** - cargo prefab dump

```diff
--- C:/Users/donfr/valheim-shadows/src/baseline-client/assembly_valheim/Character.cs
+++ C:/Users/donfr/valheim-shadows/src/client-1.0.7/assembly_valheim/Character.cs
@@ -1,4 +1,4 @@
 
 
-	[Header("Character")]
+	[UnderlinedHeader("Character", 0.5f, 0.5f, 0.5f)]
 	public string m_name = "";
```

### CharacterAnimEvent

**`CharacterAnimEvent.Awake`** - one of the six root-scoped animator lookups BodyLoader must survive

```diff
--- C:/Users/donfr/valheim-shadows/src/baseline-client/assembly_valheim/CharacterAnimEvent.cs
+++ C:/Users/donfr/valheim-shadows/src/client-1.0.7/assembly_valheim/CharacterAnimEvent.cs
@@ -14,4 +14,8 @@
 		{
 			s_ikGroundMask = LayerMask.GetMask("Default", "static_solid", "Default_small", "piece", "terrain", "vehicle");
 		}
+		if (m_loopAudioSource != null)
+		{
+			m_loopAudioSource.SetActive(value: true);
+		}
 	}
```

### Chat

**`Chat.Update`** - UIFocus: the press that opens the chat window is the one that removes the cover

```diff
--- C:/Users/donfr/valheim-shadows/src/baseline-client/assembly_valheim/Chat.cs
+++ C:/Users/donfr/valheim-shadows/src/client-1.0.7/assembly_valheim/Chat.cs
@@ -7,7 +7,7 @@
 		m_chatWindow.gameObject.SetActive(m_hideTimer < m_hideDelay);
 		if (!m_wasFocused)
 		{
-			if (Player.m_localPlayer != null && !Console.IsVisible() && !TextInput.IsVisible() && !Minimap.InTextInput() && !Menu.IsVisible() && !InventoryGui.IsVisible())
+			if (Player.m_localPlayer != null && !Console.IsVisible() && !TextInput.IsVisible() && !Minimap.InTextInput() && !Menu.IsVisible() && !InventoryGui.IsVisible() && !Hud.IsPieceSelectionVisible() && DemoMode.Disabled)
 			{
 				bool flag = ZInput.InputLayout == InputLayout.Alternative1;
 				bool button = ZInput.GetButton("JoyLBumper");
@@ -41,7 +41,7 @@
 			}
 		}
 		m_wasFocused = m_input.isFocused;
-		if (!m_input.isFocused && (Console.instance == null || !Console.instance.m_chatWindow.gameObject.activeInHierarchy))
+		if (!m_input.isFocused && (ReportUser.instance == null || !ReportUser.instance.IsReportUserWindowActive()) && (Console.instance == null || !Console.instance.m_chatWindow.gameObject.activeInHierarchy))
 		{
 			foreach (KeyValuePair<KeyCode, List<string>> bind in Terminal.m_binds)
 			{
```

### FileHelpers

**`FileHelpers.FileSource`** - World.GetWorldSavePath(FileHelpers.FileSource.Local)

```diff
--- C:/Users/donfr/valheim-shadows/src/baseline-client/assembly_utils/FileHelpers.cs
+++ C:/Users/donfr/valheim-shadows/src/client-1.0.7/assembly_utils/FileHelpers.cs
@@ -1,9 +1,10 @@
 
 
+	[Flags]
 	public enum FileSource
 	{
-		Auto,
-		Local,
-		Cloud,
-		Legacy
+		Auto = 1,
+		Local = 2,
+		Cloud = 4,
+		Legacy = 8
 	}
```

### GameCamera

**`GameCamera.AddShake`** - what a non-empty m_cameraShakeCurve would reach through a null instance

```diff
--- C:/Users/donfr/valheim-shadows/src/baseline-client/assembly_valheim/GameCamera.cs
+++ C:/Users/donfr/valheim-shadows/src/client-1.0.7/assembly_valheim/GameCamera.cs
@@ -2,10 +2,6 @@
 
 	public void AddShake(Vector3 point, float range, float strength, bool continous)
 	{
-		if (!m_cameraShakeEnabled)
-		{
-			return;
-		}
 		float num = Vector3.Distance(point, base.transform.position);
 		if (num > range)
 		{
```

**`GameCamera.UpdateMouseCapture`** - UIFocus prefix AND a Priority.First postfix

```diff
--- C:/Users/donfr/valheim-shadows/src/baseline-client/assembly_valheim/GameCamera.cs
+++ C:/Users/donfr/valheim-shadows/src/client-1.0.7/assembly_valheim/GameCamera.cs
@@ -2,23 +2,23 @@
 
 	public void UpdateMouseCapture()
 	{
-		if (ZInput.GetKey(KeyCode.LeftControl) && ZInput.GetKeyDown(KeyCode.F1))
+		if (DemoMode.Disabled && ZInput.GetKeyDown(KeyCode.F1) && ZInput.GetKey(KeyCode.LeftControl))
 		{
 			m_mouseCapture = !m_mouseCapture;
 		}
-		if (m_mouseCapture && !Hud.InRadial() && !InventoryGui.IsVisible() && !TextInput.IsVisible() && !Menu.IsVisible() && !Minimap.IsOpen() && !StoreGui.IsVisible() && !Hud.IsPieceSelectionVisible() && !PlayerCustomizaton.BarberBlocksLook() && !UnifiedPopup.IsVisible() && !ZNet.IsPasswordDialogShowing())
+		if (m_mouseCapture && !Hud.InRadial() && (!InventoryGui.IsVisible() || InventoryGui.WaitForStack()) && !TextInput.IsVisible() && !Menu.IsActive() && !Minimap.IsOpen() && !StoreGui.IsVisible() && !Hud.IsPieceSelectionVisible() && !PlayerCustomizaton.BarberBlocksLook() && !UnifiedPopup.IsVisible() && !ZNet.IsPasswordDialogShowing())
 		{
-			Cursor.lockState = CursorLockMode.Locked;
-			Cursor.visible = false;
+			ZCursor.LockState = CursorLockMode.Locked;
+			ZCursor.Hide();
 		}
 		else if (Hud.InRadial())
 		{
-			Cursor.lockState = ((!ZInput.IsMouseActive()) ? CursorLockMode.Locked : CursorLockMode.None);
-			Cursor.visible = ZInput.IsMouseActive();
+			ZCursor.LockState = ((!ZInput.IsMouseActive()) ? CursorLockMode.Locked : CursorLockMode.None);
+			ZCursor.Show();
 		}
 		else if (!Menu.IsVisible() || UnifiedPopup.IsVisible())
 		{
-			Cursor.lockState = CursorLockMode.None;
-			Cursor.visible = ZInput.IsMouseActive();
+			ZCursor.LockState = CursorLockMode.None;
+			ZCursor.Show();
 		}
 	}
```

### Heightmap

**`Heightmap.Biome`** - RandomEvent.m_biome = Heightmap.Biome.All

```diff
--- C:/Users/donfr/valheim-shadows/src/baseline-client/assembly_valheim/Heightmap.cs
+++ C:/Users/donfr/valheim-shadows/src/client-1.0.7/assembly_valheim/Heightmap.cs
@@ -1,6 +1,7 @@
 
+
 	[Flags]
 	public enum Biome
 	{
 		None = 0,
 		Meadows = 1,
@@ -12,5 +13,6 @@
 		DeepNorth = 0x40,
 		Ocean = 0x100,
 		Mistlands = 0x200,
-		All = Meadows | Swamp | Mountain | BlackForest | Plains | AshLands | DeepNorth | Ocean | Mistlands
+		All = Meadows | Swamp | Mountain | BlackForest | Plains | AshLands | DeepNorth | Ocean | Mistlands,
+		Land = 0x27F
 	}
```

### InventoryGui

**`InventoryGui.Update`** - opens on !Chat.instance.HasFocus()

```diff
--- C:/Users/donfr/valheim-shadows/src/baseline-client/assembly_valheim/InventoryGui.cs
+++ C:/Users/donfr/valheim-shadows/src/client-1.0.7/assembly_valheim/InventoryGui.cs
@@ -3,8 +3,12 @@
 	private void Update()
 	{
 		bool flag = m_animator.GetBool("visible");
-		if (!flag)
+		if (flag)
 		{
+			m_shownFrames++;
+		}
+		else
+		{
 			m_hiddenFrames++;
 		}
 		Player localPlayer = Player.m_localPlayer;
@@ -19,6 +23,17 @@
 			{
 				m_trophiesPanel.SetActive(value: false);
 			}
+			else if (m_achievementsPanel.gameObject.activeSelf && (ZInput.GetButtonDown("JoyButtonB") || ZInput.GetKeyDown(KeyCode.Escape)))
+			{
+				if (m_achievementsPanel.m_achievementDetails.activeSelf)
+				{
+					m_achievementsPanel.OnCloseAchievementDetails();
+				}
+				else
+				{
+					m_achievementsPanel.gameObject.SetActive(value: false);
+				}
+			}
 			else if (m_skillsDialog.gameObject.activeSelf && (ZInput.GetButtonDown("JoyButtonB") || ZInput.GetKeyDown(KeyCode.Escape)))
 			{
 				m_skillsDialog.OnClose();
@@ -27,9 +42,9 @@
 			{
 				m_textsDialog.gameObject.SetActive(value: false);
 			}
-			else if (m_splitPanel.gameObject.activeSelf && (ZInput.GetButtonDown("JoyButtonB") || ZInput.GetKeyDown(KeyCode.Escape)))
+			else if (m_splitDialog.IsActive && (ZInput.GetButtonDown("JoyButtonB") || ZInput.GetKeyDown(KeyCode.Escape)))
 			{
-				m_splitPanel.gameObject.SetActive(value: false);
+				HideSplitDialog();
 			}
 			else if (m_variantDialog.gameObject.activeSelf && (ZInput.GetButtonDown("JoyButtonB") || ZInput.GetKeyDown(KeyCode.Escape)))
 			{
@@ -37,7 +52,8 @@
 			}
 			else if (flag)
 			{
-				if (ZInput.GetButtonDown("Inventory") || ZInput.GetButtonDown("JoyButtonB") || ZInput.GetButtonDown("JoyButtonY") || ZInput.GetKeyDown(KeyCode.Escape) || ZInput.GetButtonDown("Use"))
+				bool flag2 = ZInput.GetButtonDown("Inventory") || ZInput.GetButtonDown("JoyButtonB") || ZInput.GetButtonDown("JoyButtonY") || ZInput.GetKeyDown(KeyCode.Escape) || ZInput.GetButtonDown("Use");
+				if ((m_shownFrames > 1) & flag2)
 				{
 					ZInput.ResetButtonStatus("Inventory");
 					ZInput.ResetButtonStatus("JoyButtonB");
@@ -46,12 +62,16 @@
 					Hide();
 				}
 			}
-			else if ((ZInput.GetButtonDown("Inventory") || ZInput.GetButtonDown("JoyButtonY")) && !Hud.InRadial())
+			else
 			{
-				ZInput.ResetButtonStatus("Inventory");
-				ZInput.ResetButtonStatus("JoyButtonY");
-				localPlayer.ShowTutorial("inventory", force: true);
-				Show(null);
+				bool flag3 = ZInput.GetButtonDown("Inventory") || ZInput.GetButtonDown("JoyButtonY");
+				if (((m_hiddenFrames > 1) & flag3) && !Hud.InRadial() && !Hud.InBuildUi())
+				{
+					ZInput.ResetButtonStatus("Inventory");
+					ZInput.ResetButtonStatus("JoyButtonY");
+					localPlayer.ShowTutorial("inventory", force: true);
+					Show(null);
+				}
 			}
 		}
 		if (flag)
```

### ItemDrop.ItemData.SharedData

**`ItemDrop.ItemData.SharedData.m_maxStackSize`** - DealApplier.AddStacks

```diff
--- C:/Users/donfr/valheim-shadows/src/baseline-client/assembly_valheim/ItemDrop.cs
+++ C:/Users/donfr/valheim-shadows/src/client-1.0.7/assembly_valheim/ItemDrop.cs
@@ -1,3 +1,4 @@
 
 
+			[Space(8f)]
 			public int m_maxStackSize = 1;
```

### Minimap

**`Minimap.IsOpen`** - a terminal panel rule

```diff
--- C:/Users/donfr/valheim-shadows/src/baseline-client/assembly_valheim/Minimap.cs
+++ C:/Users/donfr/valheim-shadows/src/client-1.0.7/assembly_valheim/Minimap.cs
@@ -2,11 +2,11 @@
 
 	public static bool IsOpen()
 	{
-		if ((bool)m_instance)
+		if ((bool)s_instance)
 		{
-			if (!m_instance.m_largeRoot.activeSelf)
+			if (!s_instance.m_largeRoot.activeSelf)
 			{
-				return m_instance.m_hiddenFrames <= 2;
+				return s_instance.m_hiddenFrames <= 2;
 			}
 			return true;
 		}
```

**`Minimap.Update`** - opens on !Chat.HasFocus() plus a still-pressed ZInput button

```diff
--- C:/Users/donfr/valheim-shadows/src/baseline-client/assembly_valheim/Minimap.cs
+++ C:/Users/donfr/valheim-shadows/src/client-1.0.7/assembly_valheim/Minimap.cs
@@ -22,7 +22,7 @@
 			{
 				return;
 			}
-			if (!TryLoadMinimapTextureData())
+			if (!TryLoadMinimapTextureData(ZNet.World.m_seed))
 			{
 				GenerateWorldMap();
 			}
@@ -48,12 +48,14 @@
 		if (m_mode == MapMode.Large)
 		{
 			m_hiddenFrames = 0;
+			m_shownFrames++;
 		}
 		else
 		{
+			m_shownFrames = 0;
 			m_hiddenFrames++;
 		}
-		bool flag = (Chat.instance == null || !Chat.instance.HasFocus()) && !Console.IsVisible() && !TextInput.IsVisible() && !Menu.IsActive() && !InventoryGui.IsVisible();
+		bool flag = (Chat.instance == null || !Chat.instance.HasFocus()) && !Console.IsVisible() && !TextInput.IsVisible() && !Menu.IsActive() && !InventoryGui.IsVisible() && !Hud.instance.m_buildUi.SearchFieldFocused;
 		if (flag)
 		{
 			if (InTextInput())
@@ -66,16 +68,23 @@
 			}
 			else if (ZInput.GetButtonDown("Map") || (ZInput.GetButtonDown("JoyMap") && (!ZInput.GetButton("JoyLTrigger") || !ZInput.GetButton("JoyLBumper")) && !ZInput.GetButton("JoyAltKeys")) || (m_mode == MapMode.Large && (ZInput.GetKeyDown(KeyCode.Escape) || (ZInput.GetButtonDown("JoyMap") && (!ZInput.GetButton("JoyLTrigger") || !ZInput.GetButton("JoyLBumper"))) || ZInput.GetButtonDown("JoyButtonB"))))
 			{
+				GamepadRumble.instance.PlayGlobalSelectVibration();
 				switch (m_mode)
 				{
 				case MapMode.None:
 					SetMapMode(MapMode.Small);
 					break;
 				case MapMode.Small:
-					SetMapMode(MapMode.Large);
+					if (m_hiddenFrames > 1)
+					{
+						SetMapMode(MapMode.Large);
+					}
 					break;
 				case MapMode.Large:
-					SetMapMode(MapMode.Small);
+					if (m_shownFrames > 1)
+					{
+						SetMapMode(MapMode.Small);
+					}
 					break;
 				}
 			}
@@ -83,7 +92,7 @@
 		if (m_mode == MapMode.Large)
 		{
 			m_publicPosition.isOn = ZNet.instance.IsReferencePositionPublic();
-			m_gamepadCrosshair.gameObject.SetActive(ZInput.IsGamepadActive());
+			m_gamepadCrosshair.gameObject.SetActive(ZInput.IsExclusiveGamepadActive());
 		}
 		if (m_showSharedMapData && m_sharedMapDataFade < 1f)
 		{
```

### NpcTalk

**`NpcTalk.Start`** - root-scoped animator lookup that runs AFTER Awake

```diff
--- C:/Users/donfr/valheim-shadows/src/baseline-client/assembly_valheim/NpcTalk.cs
+++ C:/Users/donfr/valheim-shadows/src/client-1.0.7/assembly_valheim/NpcTalk.cs
@@ -6,6 +6,8 @@
 		m_monsterAI = GetComponent<MonsterAI>();
 		m_animator = GetComponentInChildren<Animator>();
 		m_nview = GetComponent<ZNetView>();
+		Character character = m_character;
+		character.m_onDeathAnim = (Action)Delegate.Combine(character.m_onDeathAnim, new Action(OnDeathAnim));
 		MonsterAI monsterAI = m_monsterAI;
 		monsterAI.m_onBecameAggravated = (Action<BaseAI.AggravatedReason>)Delegate.Combine(monsterAI.m_onBecameAggravated, new Action<BaseAI.AggravatedReason>(OnBecameAggravated));
 		InvokeRepeating("RandomTalk", UnityEngine.Random.Range(m_randomTalkInterval / 5f, m_randomTalkInterval), m_randomTalkInterval);
```

### Player

**`Player.TakeInput`** - consults Chat.HasFocus; the choke point UIFocus works through

```diff
--- C:/Users/donfr/valheim-shadows/src/baseline-client/assembly_valheim/Player.cs
+++ C:/Users/donfr/valheim-shadows/src/client-1.0.7/assembly_valheim/Player.cs
@@ -2,7 +2,7 @@
 
 	protected override bool TakeInput()
 	{
-		bool result = (!Chat.instance || !Chat.instance.HasFocus()) && !Console.IsVisible() && !TextInput.IsVisible() && !StoreGui.IsVisible() && !InventoryGui.IsVisible() && !Menu.IsVisible() && (!TextViewer.instance || !TextViewer.instance.IsVisible()) && !Minimap.IsOpen() && !GameCamera.InFreeFly() && !PlayerCustomizaton.IsBarberGuiVisible();
+		bool result = (!Chat.instance || !Chat.instance.HasFocus()) && !Console.IsVisible() && !TextInput.IsVisible() && !StoreGui.IsVisible() && !InventoryGui.IsVisible() && !Menu.IsVisible() && (!TextViewer.instance || !TextViewer.instance.IsVisible()) && !Minimap.IsOpen() && !GameCamera.InFreeFly() && !PlayerCustomizaton.IsBarberGuiVisible() && !Hud.instance.m_buildUi.SearchFieldFocused;
 		if (IsDead() || InCutscene() || IsTeleporting())
 		{
 			result = false;
```

**`Player.Update`** - reads TakeInput() before any OnGUI runs

```diff
--- C:/Users/donfr/valheim-shadows/src/baseline-client/assembly_valheim/Player.cs
+++ C:/Users/donfr/valheim-shadows/src/client-1.0.7/assembly_valheim/Player.cs
@@ -14,11 +14,16 @@
 			}
 		}
 		UpdateClothFix();
+		if (Terminal.m_showTests)
+		{
+			Terminal.m_testList["Player Deepnorth Fade"] = WorldGenerator.DeepNorthWaveFade(base.transform.position.x, base.transform.position.z).ToString("0.00") + " / " + WorldGenerator.CreateDeepNorthGap(base.transform.position.x, base.transform.position.z).ToString("0.00");
+		}
 		if (!m_nview.IsValid() || !m_nview.IsOwner())
 		{
 			return;
 		}
 		bool flag2 = TakeInput();
+		m_lastBuildStateFrames++;
 		UpdateHover();
 		bool num;
 		if (flag2)
@@ -63,23 +68,47 @@
 			{
 				if (ZInput.GetButtonDown("Hide"))
 				{
-					goto IL_028b;
+					goto IL_031d;
 				}
 				if (flag3 && !ZInput.GetButton("JoyAltKeys"))
 				{
 					num = !InPlaceMode();
-					goto IL_0289;
+					goto IL_031b;
 				}
 			}
 			else if (!InPlaceMode() & flag3)
 			{
-				num = !ZInput.GetButton("JoyAltKeys");
-				goto IL_0289;
+				num = ZInput.GetButton("JoyAltKeys");
+				goto IL_031b;
 			}
-			goto IL_02d6;
+			goto IL_0368;
 		}
-		goto IL_0496;
-		IL_028b:
+		goto IL_052b;
+		IL_031b:
+		if (num)
+		{
+			goto IL_031d;
+		}
+		goto IL_0368;
+		IL_052b:
+		UpdateControllerTriggerFeedback(flag2);
+		UpdateGyro(flag2);
+		if (m_blockReload > 0f)
+		{
+			m_blockReload -= Time.deltaTime;
+		}
+		if (m_grappling > 0f)
+		{
+			if (m_grapplingStaminaDrain > 0f && !IsOnGround())
+			{
+				UseStamina(Time.deltaTime * m_grapplingStaminaDrain);
+			}
+			m_grappling -= Time.deltaTime;
+		}
+		UpdatePlacement(flag2, Time.deltaTime);
+		UpdateStats();
+		return;
+		IL_031d:
 		if (GetRightItem() != null || GetLeftItem() != null)
 		{
 			if (!InAttack() && !InDodge())
@@ -91,14 +120,8 @@
 		{
 			ShowHandItems();
 		}
-		goto IL_02d6;
-		IL_0289:
-		if (num)
-		{
-			goto IL_028b;
-		}
-		goto IL_02d6;
-		IL_02d6:
+		goto IL_0368;
+		IL_0368:
 		if (ZInput.GetButtonDown("ToggleWalk") && !Hud.InRadial())
 		{
 			SetWalk(!GetWalk());
@@ -157,8 +180,5 @@
 		{
 			UseHotbarItem(8);
 		}
-		goto IL_0496;
-		IL_0496:
-		UpdatePlacement(flag2, Time.deltaTime);
-		UpdateStats();
+		goto IL_052b;
 	}
```

### RandEventSystem

**`RandEventSystem.SendCurrentRandomEvent`** - the 2 s broadcast of name, time and position to Everybody

```diff
--- C:/Users/donfr/valheim-shadows/src/baseline-client/assembly_valheim/RandEventSystem.cs
+++ C:/Users/donfr/valheim-shadows/src/client-1.0.7/assembly_valheim/RandEventSystem.cs
@@ -4,10 +4,10 @@
 	{
 		if (m_randomEvent != null)
 		{
-			ZRoutedRpc.instance.InvokeRoutedRPC(ZRoutedRpc.Everybody, "SetEvent", m_randomEvent.m_name, m_randomEvent.m_time, m_randomEvent.m_pos);
+			ZRoutedRpc.instance.InvokeRoutedRPC(0L, "SetEvent", m_randomEvent.m_name, m_randomEvent.m_time, m_randomEvent.m_pos);
 		}
 		else
 		{
-			ZRoutedRpc.instance.InvokeRoutedRPC(ZRoutedRpc.Everybody, "SetEvent", "", 0f, Vector3.zero);
+			ZRoutedRpc.instance.InvokeRoutedRPC(0L, "SetEvent", "", 0f, Vector3.zero);
 		}
 	}
```

### Terminal

**`Terminal.AddString`** - every line the console prints

```diff
--- C:/Users/donfr/valheim-shadows/src/baseline-client/assembly_valheim/Terminal.cs
+++ C:/Users/donfr/valheim-shadows/src/client-1.0.7/assembly_valheim/Terminal.cs
@@ -23,33 +23,40 @@
 			return;
 		}
 		string text2 = CensorShittyWords.FilterUGC(playerInfo.m_name, UGCType.CharacterName, user, 0L);
-		if (PlatformManager.DistributionPlatform.Platform == "Xbox")
+		if (!(PlatformManager.DistributionPlatform.Platform == "Xbox"))
 		{
-			IRelationsProvider relationsProvider = PlatformManager.DistributionPlatform.RelationsProvider;
-			if (relationsProvider == null)
-			{
-				ZLog.LogError($"Relations provider was unavailable when user {text2} ({user}) sent the chat message \"{text}\"! This should never happen!");
-			}
-			string displayName;
-			if (relationsProvider != null && PlatformManager.DistributionPlatform.Platform == user.m_platform && relationsProvider.TryGetUserProfile(user, out var profile) && profile.DisplayName != null)
+			IHardwareInfoProvider hardwareInfoProvider = PlatformManager.DistributionPlatform.HardwareInfoProvider;
+			if (hardwareInfoProvider == null || hardwareInfoProvider.HardwareInfo.m_category != HardwareCategory.Console)
 			{
-				if (profile.DisplayName.Length > 0)
-				{
-					text2 = text2 + " [" + profile.DisplayName + "]";
-				}
+				goto IL_0173;
 			}
-			else if (ZNet.TryGetServerAssignedDisplayName(user, out displayName))
+		}
+		IRelationsProvider relationsProvider = PlatformManager.DistributionPlatform.RelationsProvider;
+		if (relationsProvider == null)
+		{
+			ZLog.LogError($"Relations provider was unavailable when user {text2} ({user}) sent the chat message \"{text}\"! This should never happen!");
+		}
+		string displayName;
+		if (relationsProvider != null && PlatformManager.DistributionPlatform.Platform == user.m_platform && relationsProvider.TryGetUserProfile(user, out var profile) && profile.DisplayName != null)
+		{
+			if (profile.DisplayName.Length > 0)
 			{
-				if (displayName.Length > 0)
-				{
-					text2 = text2 + " [" + displayName + "]";
-				}
+				text2 = text2 + " [" + profile.DisplayName + "]";
 			}
-			else
+		}
+		else if (ZNet.TryGetServerAssignedDisplayName(user, out displayName))
+		{
+			if (displayName.Length > 0)
 			{
-				ZLog.LogWarning($"Failed to get display name for player {text2} ({user}) who sent the chat message \"{text}\"!");
+				text2 = text2 + " [" + displayName + "]";
 			}
 		}
+		else
+		{
+			ZLog.LogWarning($"Failed to get display name for player {text2} ({user}) who sent the chat message \"{text}\"!");
+		}
+		goto IL_0173;
+		IL_0173:
 		string text3 = (timestamp ? ("[" + DateTime.Now.ToString("MM-dd-yyyy HH:mm:ss") + "] ") : "");
 		text3 = text3 + "<color=orange>" + text2 + "</color>: <color=#" + ColorUtility.ToHtmlStringRGBA(white) + ">" + text + "</color>";
 		AddString(text3);
```

**`Terminal.ConsoleCommand`** - the constructor assigns into Terminal's map by lowered name, so re-registration is harmless

```diff
--- C:/Users/donfr/valheim-shadows/src/baseline-client/assembly_valheim/Terminal.cs
+++ C:/Users/donfr/valheim-shadows/src/client-1.0.7/assembly_valheim/Terminal.cs
@@ -18,6 +18,8 @@
 
 		public bool RemoteCommand;
 
+		public bool HideBehindDevCommands;
+
 		public bool OnlyAdmin;
 
 		private ConsoleEventFailable actionFailable;
@@ -30,7 +32,7 @@
 
 		private bool m_alwaysRefreshTabOptions;
 
-		public ConsoleCommand(string command, string description, ConsoleEventFailable action, bool isCheat = false, bool isNetwork = false, bool onlyServer = false, bool isSecret = false, bool allowInDevBuild = false, ConsoleOptionsFetcher optionsFetcher = null, bool alwaysRefreshTabOptions = false, bool remoteCommand = false, bool onlyAdmin = false)
+		public ConsoleCommand(string command, string description, ConsoleEventFailable action, bool isCheat = false, bool isNetwork = false, bool onlyServer = false, bool isSecret = false, bool allowInDevBuild = false, bool hideBehindDevCommands = false, ConsoleOptionsFetcher optionsFetcher = null, bool alwaysRefreshTabOptions = false, bool remoteCommand = false, bool onlyAdmin = false)
 		{
 			commands[command.ToLower()] = this;
 			Command = command;
@@ -44,10 +46,11 @@
 			m_tabOptionsFetcher = optionsFetcher;
 			m_alwaysRefreshTabOptions = alwaysRefreshTabOptions;
 			RemoteCommand = remoteCommand;
+			HideBehindDevCommands = hideBehindDevCommands;
 			OnlyAdmin = onlyAdmin;
 		}
 
-		public ConsoleCommand(string command, string description, ConsoleEvent action, bool isCheat = false, bool isNetwork = false, bool onlyServer = false, bool isSecret = false, bool allowInDevBuild = false, ConsoleOptionsFetcher optionsFetcher = null, bool alwaysRefreshTabOptions = false, bool remoteCommand = false, bool onlyAdmin = false)
+		public ConsoleCommand(string command, string description, ConsoleEvent action, bool isCheat = false, bool isNetwork = false, bool onlyServer = false, bool isSecret = false, bool allowInDevBuild = false, bool hideBehindDevCommands = false, ConsoleOptionsFetcher optionsFetcher = null, bool alwaysRefreshTabOptions = false, bool remoteCommand = false, bool onlyAdmin = false)
 		{
 			commands[command.ToLower()] = this;
 			Command = command;
@@ -61,6 +64,7 @@
 			m_tabOptionsFetcher = optionsFetcher;
 			m_alwaysRefreshTabOptions = alwaysRefreshTabOptions;
 			RemoteCommand = remoteCommand;
+			HideBehindDevCommands = hideBehindDevCommands;
 			OnlyAdmin = onlyAdmin;
 		}
 
@@ -75,6 +79,11 @@
 
 		public void RunAction(ConsoleEventArgs args)
 		{
+			if (IsCheat && !Achievements.IsCheatedAtAll() && args[0].ToLower() != "confirmcheats")
+			{
+				args.Context.AddString(Localization.instance.Localize("$achievements_confirm_cheat"));
+				return;
+			}
 			if (args.Length >= 2)
 			{
 				List<string> tabOptions = GetTabOptions();
@@ -114,7 +123,8 @@
 					playerProfile.m_usedCheats = true;
 					playerProfile.IncrementStat(PlayerStatType.Cheats);
 				}
-				playerProfile.m_knownCommands.IncrementOrSet(args[0].ToLower());
+				playerProfile.m_playerStats[1].m_knownCommands.IncrementOrSet(args[0].ToLower());
+				playerProfile.m_playerStats[Achievements.GetCurrentAchievementDifficultyIndex()].m_knownCommands.IncrementOrSet(args[0].ToLower());
 			}
 		}
 
@@ -137,7 +147,7 @@
 
 		public bool IsValid(Terminal context, bool skipAllowedCheck = false)
 		{
-			if ((!IsCheat || context.IsCheatsEnabled()) && (context.isAllowedCommand(this) | skipAllowedCheck) && (!IsNetwork || (bool)ZNet.instance))
+			if ((!IsCheat || context.IsCheatsEnabled()) && (!HideBehindDevCommands || context.IsCheatsEnabled()) && (context.isAllowedCommand(this) | skipAllowedCheck) && (!IsNetwork || (bool)ZNet.instance))
 			{
 				if (OnlyServer)
 				{
```

**`Terminal.ConsoleEventArgs`** - Args and Context

```diff
--- C:/Users/donfr/valheim-shadows/src/baseline-client/assembly_valheim/Terminal.cs
+++ C:/Users/donfr/valheim-shadows/src/client-1.0.7/assembly_valheim/Terminal.cs
@@ -9,17 +9,20 @@
 
 		public Terminal Context;
 
+		public ConsoleCommand Commmand;
+
 		public int Length => Args.Length;
 
 		public string this[int i] => Args[i];
 
-		public ConsoleEventArgs(string line, Terminal context)
+		public ConsoleEventArgs(string line, Terminal context, ConsoleCommand cmd)
 		{
 			Context = context;
 			FullLine = line;
 			int num = line.IndexOf(' ');
 			ArgsAll = ((num > 0) ? line.Substring(num + 1) : "");
 			Args = line.Split(' ');
+			Commmand = cmd;
 		}
 
 		public int TryParameterInt(int parameterIndex, int defaultValue = 1)
```

**`Terminal.InitTerminal`** - our postfix registers the cargo console

```diff
--- C:/Users/donfr/valheim-shadows/src/baseline-client/assembly_valheim/Terminal.cs
+++ C:/Users/donfr/valheim-shadows/src/client-1.0.7/assembly_valheim/Terminal.cs
@@ -52,10 +52,17 @@
 			}
 			m_cheat = !m_cheat;
 			args.Context?.AddString("Dev commands: " + m_cheat);
-			args.Context?.AddString("WARNING: using any dev commands is not recommended and is done at your own risk.");
+			if (m_cheat)
+			{
+				args.Context?.AddString("WARNING: using any dev commands is not recommended and is done at your own risk.");
+			}
 			Gogan.LogEvent("Cheat", "CheatsEnabled", m_cheat.ToString(), 0L);
 			args.Context.updateCommandList();
 		}, isCheat: false, isNetwork: false, onlyServer: false, isSecret: true);
+		new ConsoleCommand("confirmcheats", "", delegate(ConsoleEventArgs args)
+		{
+			args.Context?.AddString(Localization.instance.Localize("$achievements_permanently_cheated_character"));
+		}, isCheat: true, isNetwork: false, onlyServer: true, isSecret: true, allowInDevBuild: false, hideBehindDevCommands: false, null, alwaysRefreshTabOptions: false, remoteCommand: true);
 		new ConsoleCommand("hidebetatext", "", delegate
 		{
 			if ((bool)Hud.instance)
@@ -101,12 +108,12 @@
 			long totalMemory2 = GC.GetTotalMemory(forceFullCollection: true);
 			long num2 = totalMemory2 - totalMemory;
 			args.Context.AddString("GC collect, Delta: " + (num2 / 1048576).ToString("0") + "mb   Total left:" + (totalMemory2 / 1048576).ToString("0") + "mb");
-		}, isCheat: true);
+		}, isCheat: false, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: true);
 		new ConsoleCommand("cr", "unloads unused assets", delegate(ConsoleEventArgs args)
 		{
 			args.Context.AddString("Unloading unused assets");
 			Game.instance.CollectResources(displayMessage: true);
-		}, isCheat: true);
+		}, isCheat: false, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: true);
 		new ConsoleCommand("fov", "changes camera field of view", delegate(ConsoleEventArgs args)
 		{
 			Camera mainCamera = Utils.GetMainCamera();
@@ -171,10 +178,17 @@
 			TerrainComp.UpgradeTerrain();
 			Heightmap.UpdateTerrainAlpha();
 		}, isCheat: false, isNetwork: true);
-		new ConsoleCommand("genloc", "regenerate all locations.", delegate
+		new ConsoleCommand("genloc", "regenerate all locations.", delegate(ConsoleEventArgs args)
 		{
-			ZoneSystem.instance.GenerateLocations();
-		}, isCheat: false, isNetwork: false, onlyServer: true, isSecret: false, allowInDevBuild: false, null, alwaysRefreshTabOptions: false, remoteCommand: true);
+			if (args.Length >= 2 && args[1].ToLower() == "alt")
+			{
+				ZNet.instance.GetWorld().m_biomeData.GenerateAltBiomes();
+			}
+			else
+			{
+				ZoneSystem.instance.GenerateLocations();
+			}
+		}, isCheat: false, isNetwork: false, onlyServer: true, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: false, null, alwaysRefreshTabOptions: false, remoteCommand: true);
 		new ConsoleCommand("players", "[nr] - force diffuculty scale ( 0 = reset)", delegate(ConsoleEventArgs args)
 		{
 			if (args.Length < 2)
@@ -187,7 +201,7 @@
 				args.Context.AddString("Setting players to " + value);
 			}
 			return true;
-		}, isCheat: true, isNetwork: false, onlyServer: true, isSecret: false, allowInDevBuild: false, null, alwaysRefreshTabOptions: false, remoteCommand: true);
+		}, isCheat: true, isNetwork: false, onlyServer: true, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: false, null, alwaysRefreshTabOptions: false, remoteCommand: true);
 		new ConsoleCommand("exclusivefullscreen", "changes window mode to exclusive fullscreen, or back to borderless", delegate
 		{
 			if (Screen.fullScreenMode != FullScreenMode.ExclusiveFullScreen)
@@ -203,14 +217,22 @@
 		{
 			if (args.Length >= 2)
 			{
-				ZoneSystem.instance.SetGlobalKey(args.FullLine.Substring(args[0].Length + 1));
-				args.Context.AddString("Setting global key " + args[1]);
+				string text = args.FullLine.Substring(args[0].Length + 1);
+				if (ServerOptionsGUI.SetKeyAndValueIsCheat(text) && !Achievements.IsCheatedAtAll())
+				{
+					args.Context.AddString("Did not apply key. " + Localization.instance.Localize("$achievements_confirm_cheat") + ". If you want to use this command to set non-cheated world keys, keep in mind that only keys and values that are also possible to set via the World Modifiers GUI do not count as cheats.");
+				}
+				else
+				{
+					ZoneSystem.instance.SetGlobalKey(text);
+					args.Context.AddString("Setting global key " + args[1]);
+				}
 			}
 			else
 			{
 				args.Context.AddString("Syntax: setkey [key]");
 			}
-		}, isCheat: true, isNetwork: false, onlyServer: true, isSecret: false, allowInDevBuild: false, delegate
+		}, isCheat: false, isNetwork: false, onlyServer: true, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: true, delegate
 		{
 			List<string> list = Enum.GetNames(typeof(GlobalKeys)).ToList();
 			list.Remove(GlobalKeys.NonServerOption.ToString());
@@ -227,18 +249,18 @@
 			{
 				args.Context.AddString("Syntax: setkey [key]");
 			}
-		}, isCheat: true, isNetwork: false, onlyServer: true, isSecret: false, allowInDevBuild: false, () => (!ZoneSystem.instance) ? null : ZoneSystem.instance.GetGlobalKeys(), alwaysRefreshTabOptions: true, remoteCommand: true);
+		}, isCheat: false, isNetwork: false, onlyServer: true, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: true, () => (!ZoneSystem.instance) ? null : ZoneSystem.instance.GetGlobalKeys(), alwaysRefreshTabOptions: true, remoteCommand: true);
 		new ConsoleCommand("resetkeys", "[name]", delegate(ConsoleEventArgs args)
 		{
 			ZoneSystem.instance.ResetGlobalKeys();
 			Player.m_localPlayer?.ResetUniqueKeys();
 			args.Context.AddString("Global and player keys cleared");
-		}, isCheat: true, isNetwork: false, onlyServer: true, isSecret: false, allowInDevBuild: false, null, alwaysRefreshTabOptions: false, remoteCommand: true);
+		}, isCheat: true, isNetwork: false, onlyServer: true, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: true, null, alwaysRefreshTabOptions: false, remoteCommand: true);
 		new ConsoleCommand("resetworldkeys", "[name] Resets all world modifiers to default", delegate(ConsoleEventArgs args)
 		{
 			ZoneSystem.instance.ResetWorldKeys();
 			args.Context.AddString("Server keys cleared");
-		}, isCheat: false, isNetwork: false, onlyServer: true, isSecret: false, allowInDevBuild: false, null, alwaysRefreshTabOptions: false, remoteCommand: true);
+		}, isCheat: false, isNetwork: false, onlyServer: true, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: false, null, alwaysRefreshTabOptions: false, remoteCommand: true);
 		new ConsoleCommand("setworldpreset", "[name] Resets all world modifiers to a named preset", delegate(ConsoleEventArgs args)
 		{
 			if (!Enum.TryParse<WorldPresets>(args[1], ignoreCase: true, out var result))
@@ -250,7 +272,7 @@
 			ServerOptionsGUI.m_instance.SetPreset(ZNet.World, result);
 			ServerOptionsGUI.m_instance.SetKeys(ZNet.World);
 			return true;
-		}, isCheat: false, isNetwork: false, onlyServer: true, isSecret: false, allowInDevBuild: false, () => Enum.GetNames(typeof(WorldPresets)).ToList(), alwaysRefreshTabOptions: false, remoteCommand: true);
+		}, isCheat: false, isNetwork: false, onlyServer: true, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: false, () => Enum.GetNames(typeof(WorldPresets)).ToList(), alwaysRefreshTabOptions: false, remoteCommand: true);
 		new ConsoleCommand("setworldmodifier", "[name] [value] Sets a world modifier value", delegate(ConsoleEventArgs args)
 		{
 			if (!Enum.TryParse<WorldModifiers>(args[1], ignoreCase: true, out var result) || !Enum.TryParse<WorldModifierOption>(args[2], ignoreCase: true, out var result2))
@@ -261,7 +283,7 @@
 			ServerOptionsGUI.m_instance.SetPreset(ZNet.World, result, result2);
 			ServerOptionsGUI.m_instance.SetKeys(ZNet.World);
 			return true;
-		}, isCheat: false, isNetwork: false, onlyServer: true, isSecret: false, allowInDevBuild: false, () => Enum.GetNames(typeof(WorldModifiers)).ToList(), alwaysRefreshTabOptions: false, remoteCommand: true);
+		}, isCheat: false, isNetwork: false, onlyServer: true, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: false, () => Enum.GetNames(typeof(WorldModifiers)).ToList(), alwaysRefreshTabOptions: false, remoteCommand: true);
 		new ConsoleCommand("setkeyplayer", "[name]", delegate(ConsoleEventArgs args)
 		{
 			if (args.Length >= 2)
@@ -273,7 +295,7 @@
 			{
 				args.Context.AddString("Syntax: setkey [key]");
 			}
-		}, isCheat: true, isNetwork: false, onlyServer: true, isSecret: false, allowInDevBuild: false, () => Enum.GetNames(typeof(PlayerKeys)).ToList());
+		}, isCheat: true, isNetwork: false, onlyServer: true, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: false, () => Enum.GetNames(typeof(PlayerKeys)).ToList());
 		new ConsoleCommand("removekeyplayer", "[name]", delegate(ConsoleEventArgs args)
 		{
 			if (args.Length >= 2)
@@ -285,7 +307,7 @@
 			{
 				args.Context.AddString("Syntax: setkey [key]");
 			}
-		}, isCheat: true, isNetwork: false, onlyServer: true, isSecret: false, allowInDevBuild: false, () => (!Player.m_localPlayer) ? null : Player.m_localPlayer.GetUniqueKeys(), alwaysRefreshTabOptions: true);
+		}, isCheat: true, isNetwork: false, onlyServer: true, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: false, () => (!Player.m_localPlayer) ? null : Player.m_localPlayer.GetUniqueKeys(), alwaysRefreshTabOptions: true);
 		new ConsoleCommand("listkeys", "", delegate(ConsoleEventArgs args)
 		{
 			List<string> globalKeys = ZoneSystem.instance.GetGlobalKeys();
@@ -321,7 +343,7 @@
 					args.Context.AddString("  " + item4);
 				}
 			}
-		}, isCheat: true, isNetwork: false, onlyServer: true, isSecret: false, allowInDevBuild: false, null, alwaysRefreshTabOptions: false, remoteCommand: true);
+		}, isCheat: true, isNetwork: false, onlyServer: true, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: false, null, alwaysRefreshTabOptions: false, remoteCommand: true);
 		new ConsoleCommand("sortcraft", "[type] sorts crafting lists according to setting", delegate(ConsoleEventArgs args)
 		{
 			Player.m_localPlayer.RemoveUniqueKeyValue("sortcraft");
@@ -334,7 +356,7 @@
 			{
 				args.Context.AddString("List sorting reset");
 			}
-		}, isCheat: false, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, () => Enum.GetNames(typeof(InventoryGui.SortMethod)).ToList());
+		}, isCheat: false, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: false, () => Enum.GetNames(typeof(InventoryGui.SortMethod)).ToList());
 		new ConsoleCommand("debugmode", "fly mode", delegate(ConsoleEventArgs args)
 		{
 			Player.m_debugMode = !Player.m_debugMode;
@@ -373,7 +395,7 @@
 			{
 				args.Context.AddString("Syntax: raiseskill [skill] [amount]");
 			}
-		}, isCheat: true, isNetwork: false, onlyServer: true, isSecret: false, allowInDevBuild: false, delegate
+		}, isCheat: true, isNetwork: false, onlyServer: true, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: false, delegate
 		{
 			List<string> list = Enum.GetNames(typeof(Skills.SkillType)).ToList();
 			list.Remove(Skills.SkillType.None.ToString());
@@ -390,7 +412,7 @@
 			{
 				args.Context.AddString("Syntax: resetskill [skill]");
 			}
-		}, isCheat: true, isNetwork: false, onlyServer: true, isSecret: false, allowInDevBuild: false, delegate
+		}, isCheat: true, isNetwork: false, onlyServer: true, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: false, delegate
 		{
 			List<string> list = Enum.GetNames(typeof(Skills.SkillType)).ToList();
 			list.Remove(Skills.SkillType.None.ToString());
@@ -399,7 +421,7 @@
 		new ConsoleCommand("sleep", "skips to next morning", delegate
 		{
 			EnvMan.instance.SkipToMorning();
-		}, isCheat: true, isNetwork: false, onlyServer: true, isSecret: false, allowInDevBuild: false, null, alwaysRefreshTabOptions: false, remoteCommand: true);
+		}, isCheat: true, isNetwork: false, onlyServer: true, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: false, null, alwaysRefreshTabOptions: false, remoteCommand: true);
 		new ConsoleCommand("stats", "shows player stats", delegate(ConsoleEventArgs args)
 		{
 			if ((bool)Game.instance)
@@ -410,49 +432,147 @@
 				{
 					args.Context.AddString("Cheater!");
 				}
-				foreach (KeyValuePair<PlayerStatType, float> stat in playerProfile.m_playerStats.m_stats)
+				for (int i = 0; i < 10; i++)
 				{
-					if (PlayerProfile.m_statTypeDates.TryGetValue(stat.Key, out var value))
+					args.Context.AddString($"Difficulty Category: {(DifficultyRequirement)i}");
+					foreach (KeyValuePair<PlayerStatType, float> stat2 in playerProfile.m_playerStats[i].m_stats)
 					{
-						args.Context.AddString("  " + value);
+						if (PlayerProfile.m_statTypeDates.TryGetValue(stat2.Key, out var value))
+						{
+							args.Context.AddString("  " + value);
+						}
+						if (stat2.Value != 0f)
+						{
+							args.Context.AddString($"    {stat2.Key}: {stat2.Value}");
+						}
 					}
-					args.Context.AddString($"    {stat.Key}: {stat.Value}");
-				}
-				args.Context.AddString("Known worlds:");
-				foreach (KeyValuePair<string, float> knownWorld in playerProfile.m_knownWorlds)
-				{
-					args.Context.AddString("  " + knownWorld.Key + ": " + TimeSpan.FromSeconds(knownWorld.Value).ToString("c"));
-				}
-				args.Context.AddString("Enemies:");
-				foreach (KeyValuePair<string, float> enemyStat in playerProfile.m_enemyStats)
-				{
-					args.Context.AddString($"  {Localization.instance.Localize(enemyStat.Key)}: {enemyStat.Value}");
+					args.Context.AddString("Known worlds:");
+					foreach (KeyValuePair<string, float> knownWorld in playerProfile.m_playerStats[i].m_knownWorlds)
+					{
+						args.Context.AddString("  " + knownWorld.Key + ": " + TimeSpan.FromSeconds(knownWorld.Value).ToString("c"));
+					}
+					args.Context.AddString("Enemies:");
+					for (int j = 0; j < 5; j++)
+					{
+						args.Context.AddString($" {(KillModifiers)j}:");
+						foreach (KeyValuePair<string, float> item5 in playerProfile.m_playerStats[i].m_enemyStats[j])
+						{
+							args.Context.AddString($"  {Localization.instance.Localize(item5.Key)}: {item5.Value}");
+						}
+					}
+					args.Context.AddString("Items found:");
+					foreach (KeyValuePair<string, float> itemPickupStat in playerProfile.m_playerStats[i].m_itemPickupStats)
+					{
+						args.Context.AddString($"  {Localization.instance.Localize(itemPickupStat.Key)}: {itemPickupStat.Value}");
+					}
+					args.Context.AddString("Crafts:");
+					foreach (KeyValuePair<string, float> itemCraftStat in playerProfile.m_playerStats[i].m_itemCraftStats)
+					{
+						args.Context.AddString($"  {Localization.instance.Localize(itemCraftStat.Key)}: {itemCraftStat.Value}");
+					}
+					args.Context.AddString("Pickables:");
+					foreach (KeyValuePair<string, float> pickableStat in playerProfile.m_playerStats[i].m_pickableStats)
+					{
+						args.Context.AddString($"  {Localization.instance.Localize(pickableStat.Key)}: {pickableStat.Value}");
+					}
+					if (args.Length > 1)
+					{
+						args.Context.AddString("Known world keys:");
+						foreach (KeyValuePair<string, float> knownWorldKey in playerProfile.m_playerStats[i].m_knownWorldKeys)
+						{
+							args.Context.AddString("  " + knownWorldKey.Key + ": " + TimeSpan.FromSeconds(knownWorldKey.Value).ToString("c"));
+						}
+						args.Context.AddString("Used commands:");
+						foreach (KeyValuePair<string, float> knownCommand in playerProfile.m_playerStats[i].m_knownCommands)
+						{
+							args.Context.AddString($"  {knownCommand.Key}: {knownCommand.Value}");
+						}
+					}
 				}
-				args.Context.AddString("Items found:");
-				foreach (KeyValuePair<string, float> itemPickupStat in playerProfile.m_itemPickupStats)
+			}
+		}, isCheat: false, isNetwork: false, onlyServer: true);
+		new ConsoleCommand("achievements", "shows player achievement info", delegate(ConsoleEventArgs args)
+		{
+			if ((bool)Game.instance)
+			{
+				PlayerProfile playerProfile = Game.instance.GetPlayerProfile();
+				int num2 = 0;
+				int num3 = 0;
+				foreach (AchievementList achievementList in Achievements.m_instance.m_achievementLists)
 				{
-					args.Context.AddString($"  {Localization.instance.Localize(itemPickupStat.Key)}: {itemPickupStat.Value}");
+					foreach (Achievement achievement in achievementList.m_achievements)
+					{
+						if (achievement.m_unlocked)
+						{
+							num2++;
+						}
+						else
+						{
+							num3++;
+						}
+					}
 				}
-				args.Context.AddString("Crafts:");
-				foreach (KeyValuePair<string, float> itemCraftStat in playerProfile.m_itemCraftStats)
+				args.Context.AddString($"Achievement Unlocked {num2}:");
+				foreach (AchievementList achievementList2 in Achievements.m_instance.m_achievementLists)
 				{
-					args.Context.AddString($"  {Localization.instance.Localize(itemCraftStat.Key)}: {itemCraftStat.Value}");
+					foreach (Achievement achievement2 in achievementList2.m_achievements)
+					{
+						if (achievement2.m_unlocked)
+						{
+							args.Context.AddString(achievement2.m_name ?? "");
+						}
+					}
 				}
-				if (args.Length > 1)
+				args.Context.AddString($"Achievement Locked {num3}:");
+				foreach (AchievementList achievementList3 in Achievements.m_instance.m_achievementLists)
 				{
-					args.Context.AddString("Known world keys:");
-					foreach (KeyValuePair<string, float> knownWorldKey in playerProfile.m_knownWorldKeys)
+					foreach (Achievement achievement3 in achievementList3.m_achievements)
 					{
-						args.Context.AddString("  " + knownWorldKey.Key + ": " + TimeSpan.FromSeconds(knownWorldKey.Value).ToString("c"));
+						if (!achievement3.m_unlocked)
+						{
+							args.Context.AddString(achievement3.m_name ?? "");
+						}
 					}
-					args.Context.AddString("Used commands:");
-					foreach (KeyValuePair<string, float> knownCommand in playerProfile.m_knownCommands)
+				}
+				for (int i = 0; i < 10; i++)
+				{
+					args.Context.AddString($"\nAchievement stats ({(DifficultyRequirement)i})");
+					foreach (KeyValuePair<PlayerStatType, float> stat3 in playerProfile.m_playerStats[i].m_stats)
 					{
-						args.Context.AddString($"  {knownCommand.Key}: {knownCommand.Value}");
+						if (stat3.Value > 0f)
+						{
+							if (PlayerProfile.m_statTypeDates.TryGetValue(stat3.Key, out var value))
+							{
+								args.Context.AddString("  " + value);
+							}
+							args.Context.AddString($"    {stat3.Key}: {stat3.Value}");
+						}
 					}
 				}
 			}
 		}, isCheat: false, isNetwork: false, onlyServer: true);
+		new ConsoleCommand("getstat", "get specific stat", delegate(ConsoleEventArgs args)
+		{
+			if (args.Length < 2)
+			{
+				return false;
+			}
+			if (!Enum.TryParse<PlayerStatType>(args.Args[1], ignoreCase: true, out var stat))
+			{
+				args.Context.AddString("Stat not found");
+				return true;
+			}
+			PlayerProfile playerProfile = Game.instance.GetPlayerProfile();
+			PlatformManager.DistributionPlatform?.AchievementManager.GetProgress(stat.ToString(), delegate(bool success, float progress)
+			{
+				args.Context.AddString($"{stat}: remote: {progress}");
+			});
+			for (int num2 = 0; num2 < 10; num2++)
+			{
+				args.Context.AddString($"   achievement ({(DifficultyRequirement)num2}): {playerProfile.m_playerStats[num2][stat]}");
+			}
+			return true;
+		}, isCheat: false, isNetwork: false, onlyServer: true, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: false, () => Enum.GetNames(typeof(PlayerStatType)).ToList());
 		new ConsoleCommand("skiptime", "[gameseconds] skips head in seconds", delegate(ConsoleEventArgs args)
 		{
 			double timeSeconds = ZNet.instance.GetTimeSeconds();
@@ -460,18 +580,18 @@
 			timeSeconds += (double)num2;
 			ZNet.instance.SetNetTime(timeSeconds);
 			args.Context.AddString("Skipping " + num2.ToString("0") + "s , Day:" + EnvMan.instance.GetDay(timeSeconds));
-		}, isCheat: true, isNetwork: false, onlyServer: true, isSecret: false, allowInDevBuild: false, null, alwaysRefreshTabOptions: false, remoteCommand: true);
+		}, isCheat: true, isNetwork: false, onlyServer: true, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: false, null, alwaysRefreshTabOptions: false, remoteCommand: true);
 		new ConsoleCommand("time", "shows current time", delegate(ConsoleEventArgs args)
 		{
 			double timeSeconds = ZNet.instance.GetTimeSeconds();
 			bool flag = EnvMan.CanSleep();
 			args.Context.AddString(string.Format("{0} sec, Day: {1} ({2}), {3}, Session start: {4}", timeSeconds.ToString("0.00"), EnvMan.instance.GetDay(timeSeconds), EnvMan.instance.GetDayFraction().ToString("0.00"), flag ? "Can sleep" : "Can NOT sleep", ZoneSystem.instance.TimeSinceStart()));
-		}, isCheat: true);
+		}, isCheat: false, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: true);
 		new ConsoleCommand("maxfps", "[FPS] sets fps limit", delegate(ConsoleEventArgs args)
 		{
 			if (args.TryParameterInt(1, out var value))
 			{
-				GraphicsSettingsState settings = GraphicsSettingsManager.Instance.GetCurrentSettingsWithCurrentPresetApplied(includeBackground: true);
+				GraphicsSettingsState settings = GraphicsSettingsManager.Instance.ActiveSettings;
 				settings.m_presentSettings.m_fpsLimit = value;
 				GraphicsSettingsManager.Instance.SaveAndApplyGraphicsSettingsCustom(ref settings);
 				return true;
@@ -520,7 +640,7 @@
 			}
 			RandEventSystem.instance.SetRandomEventByName(text, Player.m_localPlayer.transform.position);
 			return true;
-		}, isCheat: true, isNetwork: false, onlyServer: true, isSecret: false, allowInDevBuild: false, delegate
+		}, isCheat: true, isNetwork: false, onlyServer: true, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: false, delegate
 		{
 			List<string> list = new List<string>();
 			foreach (RandomEvent @event in RandEventSystem.instance.m_events)
@@ -551,7 +671,7 @@
 				}
 			}
 			Player.m_localPlayer.Message(MessageHud.MessageType.TopLeft, "Removed item drops: " + num2);
-		}, isCheat: true);
+		}, isCheat: false, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: true);
 		new ConsoleCommand("removefish", "remove all fish", delegate
 		{
 			int num2 = 0;
@@ -566,35 +686,35 @@
 				}
 			}
 			Player.m_localPlayer.Message(MessageHud.MessageType.TopLeft, "Removed fish: " + num2);
-		}, isCheat: true);
+		}, isCheat: false, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: true);
 		new ConsoleCommand("printcreatures", "shows counts and levels of active creatures", delegate(ConsoleEventArgs args)
 		{
 			Dictionary<string, Dictionary<int, int>> counts = new Dictionary<string, Dictionary<int, int>>();
 			GetInfo(Character.GetAllCharacters());
 			GetInfo(UnityEngine.Object.FindObjectsOfType<RandomFlyingBird>());
 			GetInfo(UnityEngine.Object.FindObjectsOfType<Fish>());
-			foreach (KeyValuePair<string, Dictionary<int, int>> item5 in counts)
+			foreach (KeyValuePair<string, Dictionary<int, int>> item6 in counts)
 			{
-				string text = Localization.instance.Localize(item5.Key) + ": ";
-				foreach (KeyValuePair<int, int> item6 in item5.Value)
+				string text = Localization.instance.Localize(item6.Key) + ": ";
+				foreach (KeyValuePair<int, int> item7 in item6.Value)
 				{
-					text += $"Level {item6.Key}: {item6.Value}, ";
+					text += $"Level {item7.Key}: {item7.Value}, ";
 				}
 				args.Context.AddString(text);
 			}
 			void GetInfo(IEnumerable collection)
 			{
-				foreach (object item7 in collection)
+				foreach (object item8 in collection)
 				{
-					if (item7 is Character character)
+					if (item8 is Character character)
 					{
 						count(character.m_name, character.GetLevel());
 					}
-					else if (item7 is RandomFlyingBird)
+					else if (item8 is RandomFlyingBird)
 					{
 						count("Bird", 1);
 					}
-					else if (item7 is Fish fish)
+					else if (item8 is Fish fish)
 					{
 						ItemDrop component = fish.GetComponent<ItemDrop>();
 						if ((object)component != null)
@@ -603,9 +723,9 @@
 						}
 					}
 				}
-				foreach (object item8 in collection)
+				foreach (object item9 in collection)
 				{
-					if (item8 is MonoBehaviour monoBehaviour)
+					if (item9 is MonoBehaviour monoBehaviour)
 					{
 						args.Context.AddString(string.Format("   {0}, Dist: {1}, Offset: {2}", monoBehaviour.name, Vector3.Distance(Player.m_localPlayer.transform.position, monoBehaviour.transform.position).ToString("0.0"), monoBehaviour.transform.position - Player.m_localPlayer.transform.position));
 					}
@@ -637,9 +757,9 @@
 				}
 			}
 			args.Context.AddString($"Total network objects found: {total}");
-			foreach (KeyValuePair<string, int> item9 in counts)
+			foreach (KeyValuePair<string, int> item10 in counts)
 			{
-				args.Context.AddString($"   {item9.Key}: {item9.Value}");
+				args.Context.AddString($"   {item10.Key}: {item10.Value}");
 			}
 			void add(string key)
 			{
@@ -668,7 +788,7 @@
 				}
 			}
 			Player.m_localPlayer.Message(MessageHud.MessageType.TopLeft, "Removed birds: " + num2);
-		}, isCheat: true);
+		}, isCheat: false, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: true);
 		new ConsoleCommand("printlocations", "shows counts of loaded locations", delegate(ConsoleEventArgs args)
 		{
 			new Dictionary<string, Dictionary<int, int>>();
@@ -687,9 +807,9 @@
 			string text = args[1].ToLower();
 			List<Tuple<object, Vector3>> list = find(text);
 			list.Sort((Tuple<object, Vector3> a, Tuple<object, Vector3> b) => Vector3.Distance(a.Item2, Player.m_localPlayer.transform.position).CompareTo(Vector3.Distance(b.Item2, Player.m_localPlayer.transform.position)));
-			foreach (Tuple<object, Vector3> item10 in list)
+			foreach (Tuple<object, Vector3> item11 in list)
 			{
-				args.Context.AddString(string.Format("   {0}, Dist: {1}, Pos: {2}", (item10.Item1 is GameObject gameObject) ? gameObject.name.ToString() : ((item10.Item1 is ZoneSystem.LocationInstance locationInstance) ? locationInstance.m_location.m_prefab.Name : "unknown"), Vector3.Distance(Player.m_localPlayer.transform.position, item10.Item2).ToString("0.0"), item10.Item2));
+				args.Context.AddString(string.Format("   {0}, Dist: {1}, Pos: {2}", (item11.Item1 is GameObject gameObject) ? gameObject.name.ToString() : ((item11.Item1 is ZoneSystem.LocationInstance locationInstance) ? locationInstance.m_location.m_prefab.Name : "unknown"), Vector3.Distance(Player.m_localPlayer.transform.position, item11.Item2).ToString("0.0"), item11.Item2));
 			}
 			foreach (Minimap.PinData findPin in args.Context.m_findPins)
 			{
@@ -705,12 +825,12 @@
 			{
 				for (int num3 = 0; num3 < num2; num3++)
 				{
-					args.Context.m_findPins.Add(Minimap.instance.AddPin(list[num3].Item2, (list[num3].Item1 is ZDO) ? Minimap.PinType.Icon2 : ((list[num3].Item1 is ZoneSystem.LocationInstance) ? Minimap.PinType.Icon1 : Minimap.PinType.Icon3), (list[num3].Item1 is ZDO zDO) ? zDO.GetString("tag") : "", save: false, isChecked: true, Player.m_localPlayer.GetPlayerID()));
+					args.Context.m_findPins.Add(Minimap.instance.AddPin(list[num3].Item2, (list[num3].Item1 is ZDO) ? Minimap.PinType.Icon2 : ((list[num3].Item1 is ZoneSystem.LocationInstance) ? Minimap.PinType.Icon1 : Minimap.PinType.Icon3), (list[num3].Item1 is ZDO zDO) ? zDO.GetString(ZDOVars.s_tag) : "", save: false, isChecked: true, Player.m_localPlayer.GetPlayerID()));
 				}
 			}
 			args.Context.AddString($"Found {list.Count} objects containing '{text}'");
 			return true;
-		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, findOpt);
+		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: false, findOpt);
 		new ConsoleCommand("findtp", "[text] [index=-1] [closerange=30] searches loaded objects and location list matching name and teleports you to the closest one outside of closerange. Specify an index to tp to any other in the found list, a minus value means index by closest.", delegate(ConsoleEventArgs args)
 		{
 			if (args.Length < 2 || Player.m_localPlayer == null)
@@ -745,7 +865,7 @@
 			}
 			args.Context.AddString($"Found {list.Count} objects containing '{text}'");
 			return true;
-		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, findOpt);
+		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: false, findOpt);
 		new ConsoleCommand("setfuel", "[amount=10] Sets all light fuel to specified amount", delegate(ConsoleEventArgs args)
 		{
 			if (Player.m_localPlayer == null)
@@ -760,12 +880,12 @@
 				((Fireplace)array2[i]).SetFuel(num2);
 			}
 			return true;
-		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, null, alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
+		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: false, null, alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
 		new ConsoleCommand("freefly", "freefly photo mode", delegate(ConsoleEventArgs args)
 		{
 			args.Context.AddString("Toggling free fly camera");
 			GameCamera.instance.ToggleFreeFly();
-		}, isCheat: true);
+		});
 		new ConsoleCommand("ffsmooth", "freefly smoothness", delegate(ConsoleEventArgs args)
 		{
 			if (args.Length <= 1)
@@ -780,7 +900,7 @@
 				return true;
 			}
 			return false;
-		}, isCheat: true);
+		});
 		new ConsoleCommand("location", "[SAVE*] spawn location (CAUTION: saving permanently disabled, *unless you specify SAVE)", delegate(ConsoleEventArgs args)
 		{
 			if (args.Length < 2)
@@ -791,9 +911,13 @@
 			Vector3 pos = Player.m_localPlayer.transform.position + Player.m_localPlayer.transform.forward * 10f;
 			ZoneSystem.instance.TestSpawnLocation(text, pos, args.Length < 3 || args[2] != "SAVE");
 			return true;
-		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, delegate
+		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: false, delegate
 		{
 			List<string> list = new List<string>();
+			if (Game.instance == null)
+			{
+				return list;
+			}
 			foreach (ZoneSystem.ZoneLocation location2 in ZoneSystem.instance.m_locations)
 			{
 				if (location2.m_prefab.IsValid)
@@ -803,6 +927,12 @@
 			}
 			return list;
 		}, alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
+		new ConsoleCommand("haslocation", "check if zone has location", delegate(ConsoleEventArgs args)
+		{
+			Vector3 position = Player.m_localPlayer.transform.position;
+			bool flag = ZoneSystem.instance.ZoneHasLocation(position);
+			args.Context.AddString($"Location in zone [{ZoneSystem.GetZone(position)}]: " + (flag ? "YES" : "NO"));
+		}, isCheat: false, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: true);
 		new ConsoleCommand("vegetation", "spawn vegetation", delegate(ConsoleEventArgs args)
 		{
 			if (args.Length < 2)
@@ -811,27 +941,27 @@
 			}
 			Vector3 p = Player.m_localPlayer.transform.position + Player.m_localPlayer.transform.forward * 2f;
 			string text = args[1].ToLower();
-			foreach (ZoneSystem.ZoneVegetation item11 in ZoneSystem.instance.m_vegetation)
+			foreach (ZoneSystem.ZoneVegetation item12 in ZoneSystem.instance.m_vegetation)
 			{
-				if (item11.m_prefab.name.ToLower() == text)
+				if (item12.m_prefab.name.ToLower() == text)
 				{
 					float y = UnityEngine.Random.Range(0, 360);
-					float num2 = UnityEngine.Random.Range(item11.m_scaleMin, item11.m_scaleMax);
-					float x = UnityEngine.Random.Range(0f - item11.m_randTilt, item11.m_randTilt);
-					float z = UnityEngine.Random.Range(0f - item11.m_randTilt, item11.m_randTilt);
+					float num2 = UnityEngine.Random.Range(item12.m_scaleMin, item12.m_scaleMax);
+					float x = UnityEngine.Random.Range(0f - item12.m_randTilt, item12.m_randTilt);
+					float z = UnityEngine.Random.Range(0f - item12.m_randTilt, item12.m_randTilt);
 					ZoneSystem.instance.GetGroundData(ref p, out var normal, out var _, out var _, out var _);
-					if (item11.m_snapToStaticSolid && ZoneSystem.instance.GetStaticSolidHeight(p, out var height, out var normal2))
+					if (item12.m_snapToStaticSolid && ZoneSystem.instance.GetStaticSolidHeight(p, out var height, out var normal2))
 					{
 						p.y = height;
 						normal = normal2;
 					}
-					if (item11.m_snapToWater)
+					if (item12.m_snapToWater)
 					{
 						p.y = 30f;
 					}
-					p.y += item11.m_groundOffset;
+					p.y += item12.m_groundOffset;
 					Quaternion identity = Quaternion.identity;
-					if (item11.m_chanceToUseGroundTilt > 0f && UnityEngine.Random.value <= item11.m_chanceToUseGroundTilt)
+					if (item12.m_chanceToUseGroundTilt > 0f && UnityEngine.Random.value <= item12.m_chanceToUseGroundTilt)
 					{
 						Quaternion quaternion = Quaternion.Euler(0f, y, 0f);
 						identity = Quaternion.LookRotation(Vector3.Cross(normal, quaternion * Vector3.forward), normal);
@@ -840,7 +970,7 @@
 					{
 						identity = Quaternion.Euler(x, y, z);
 					}
-					GameObject obj = UnityEngine.Object.Instantiate(item11.m_prefab, p, identity);
+					GameObject obj = UnityEngine.Object.Instantiate(item12.m_prefab, p, identity);
 					obj.GetComponent<ZNetView>().SetLocalScale(new Vector3(num2, num2, num2));
 					Collider[] componentsInChildren = obj.GetComponentsInChildren<Collider>();
 					foreach (Collider obj2 in componentsInChildren)
@@ -852,14 +982,14 @@
 				}
 			}
 			return "No vegeration prefab named '" + args[1] + "' found";
-		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, delegate
+		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: false, delegate
 		{
 			List<string> list = new List<string>();
-			foreach (ZoneSystem.ZoneVegetation item12 in ZoneSystem.instance.m_vegetation)
+			foreach (ZoneSystem.ZoneVegetation item13 in ZoneSystem.instance.m_vegetation)
 			{
-				if (item12.m_prefab != null)
+				if (item13.m_prefab != null)
 				{
-					list.Add(item12.m_prefab.name);
+					list.Add(item13.m_prefab.name);
 				}
 			}
 			return list;
@@ -877,7 +1007,7 @@
 				MessageHud.instance.ShowMessage(MessageHud.MessageType.Center, "Location seed set, world saving DISABLED until restart");
 			}
 			return true;
-		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, null, alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
+		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: false, null, alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
 		new ConsoleCommand("spawn", "[amount] [level] [radius] [p/e/i] - spawn something. (End word with a star (*) to create each object containing that word.) Add a 'p' after to try to pick up the spawned items, adding 'e' will try to use/equip, 'i' will only spawn and pickup if you don't have one in your inventory.", delegate(ConsoleEventArgs args)
 		{
 			if (args.Length <= 1 || !ZNetScene.instance)
@@ -963,48 +1093,53 @@
 				}
 				else
 				{
+					Player.m_localPlayer.Message(MessageHud.MessageType.TopLeft, "Spawning object " + name + ((count2 == 1) ? "" : $" x{count2}"));
 					for (int j = 0; j < count2; j++)
 					{
 						Vector3 vector = UnityEngine.Random.insideUnitSphere * ((count2 == 1) ? 0f : radius);
-						Player.m_localPlayer.Message(MessageHud.MessageType.TopLeft, "Spawning object " + name);
 						GameObject gameObject = UnityEngine.Object.Instantiate(prefab, Player.m_localPlayer.transform.position + Player.m_localPlayer.transform.forward * 2f + Vector3.up + vector, Quaternion.identity);
-						if (vals != null)
+						ZNetView component = gameObject.GetComponent<ZNetView>();
+						if ((object)component != null && component.IsValid())
 						{
-							ZNetView component = gameObject.GetComponent<ZNetView>();
-							if ((object)component != null && component.IsValid())
+							if (vals != null)
 							{
 								component.GetZDO().Set("HasFields", value: true);
-								foreach (KeyValuePair<string, object> item13 in vals)
+								foreach (KeyValuePair<string, object> item14 in vals)
 								{
-									string[] array3 = item13.Key.Split('.');
+									string[] array3 = item14.Key.Split('.');
 									if (array3.Length >= 2)
 									{
 										("HasFields" + array3[0]).GetStableHashCode();
 										component.GetZDO().Set("HasFields" + array3[0], value: true);
-										item13.Value.GetType();
-										if (item13.Value is float)
+										item14.Value.GetType();
+										if (item14.Value is float)
 										{
-											component.GetZDO().Set(item13.Key, (float)item13.Value);
+											component.GetZDO().Set(item14.Key, (float)item14.Value);
 										}
-										else if (item13.Value is int)
+										else if (item14.Value is int)
 										{
-											component.GetZDO().Set(item13.Key, (int)item13.Value);
+											component.GetZDO().Set(item14.Key, (int)item14.Value);
 										}
-										else if (item13.Value is bool)
+										else if (item14.Value is bool)
 										{
-											component.GetZDO().Set(item13.Key, (bool)item13.Value);
+											component.GetZDO().Set(item14.Key, (bool)item14.Value);
 										}
 										else
 										{
-											component.GetZDO().Set(item13.Key, item13.Value.ToString());
+											component.GetZDO().Set(item14.Key, item14.Value.ToString());
 										}
 									}
 								}
 								component.LoadFields();
 							}
+							component.GetZDO().Set(ZDOVars.s_cheated, !PlayerProfile.s_bypassCheatChecks);
 						}
 						ItemDrop component2 = gameObject.GetComponent<ItemDrop>();
-						ItemDrop.OnCreateNew(gameObject);
+						ItemDrop.OnCreateNew(gameObject, !PlayerProfile.s_bypassCheatChecks);
+						if ((bool)component2)
+						{
+							component2.m_itemData.m_durability = component2.m_itemData.GetMaxDurability();
+						}
 						if (level > 1)
 						{
 							if ((bool)component2)
@@ -1044,7 +1179,7 @@
 					}
 				}
 			}
-		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, () => (!ZNetScene.instance) ? new List<string>() : ZNetScene.instance.GetPrefabNames(), alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
+		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: false, () => (!ZNetScene.instance) ? new List<string>() : ZNetScene.instance.GetPrefabNames(), alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
 		new ConsoleCommand("catch", "[fishname] [level] simulates catching a fish", delegate(ConsoleEventArgs args)
 		{
 			string text = args[1];
@@ -1070,7 +1205,7 @@
 			string msg = FishingFloat.Catch(componentInChildren, Player.m_localPlayer);
 			Player.m_localPlayer.Message(MessageHud.MessageType.Center, msg);
 			return true;
-		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, () => new List<string>
+		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: false, () => new List<string>
 		{
 			"Fish1", "Fish2", "Fish3", "Fish4_cave", "Fish5", "Fish6", "Fish7", "Fish8", "Fish9", "Fish10",
 			"Fish11", "Fish12"
@@ -1083,15 +1218,15 @@
 				return true;
 			}
 			return "Specify name of itemset.";
-		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, () => ItemSets.instance.GetSetNames(), alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
+		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: false, () => ItemSets.instance.GetSetNames(), alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
 		new ConsoleCommand("pos", "print current player position", delegate(ConsoleEventArgs args)
 		{
 			Player localPlayer = Player.m_localPlayer;
 			if ((bool)localPlayer && (bool)ZoneSystem.instance)
 			{
-				args.Context?.AddString(string.Format("Player position (X,Y,Z): {0} (Zone: {1})", localPlayer.transform.position.ToString("F0"), ZoneSystem.GetZone(localPlayer.transform.position)));
+				args.Context?.AddString(string.Format("Player position (X,Y,Z): {0} , Zone: {1}, Center dist: {2}", localPlayer.transform.position.ToString("F0"), ZoneSystem.GetZone(localPlayer.transform.position), Utils.DistanceXZ(Vector3.zero, localPlayer.transform.position)));
 			}
-		}, isCheat: true);
+		}, isCheat: false, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: true);
 		new ConsoleCommand("recall", "[*name] recalls players to you, optionally that match given name", delegate(ConsoleEventArgs args)
 		{
 			foreach (ZNetPeer peer in ZNet.instance.GetPeers())
@@ -1101,7 +1236,7 @@
 					Chat.instance.TeleportPlayer(peer.m_uid, Player.m_localPlayer.transform.position, Player.m_localPlayer.transform.rotation, distantTeleport: true);
 				}
 			}
-		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, null, alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
+		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: false, null, alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
 		new ConsoleCommand("goto", "[x,z] - teleport", delegate(ConsoleEventArgs args)
 		{
 			if (args.Length < 3 || !args.TryParameterInt(1, out var value) || !args.TryParameterInt(2, out var value2))
@@ -1118,7 +1253,7 @@
 			}
 			Gogan.LogEvent("Cheat", "Goto", "", 0L);
 			return true;
-		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: true, null, alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
+		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: true, hideBehindDevCommands: false, null, alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
 		new ConsoleCommand("exploremap", "explore entire map", delegate
 		{
 			Minimap.instance.ExploreAll();
@@ -1144,32 +1279,32 @@
 					ZPlayFabSocket.ScheduleResetParty();
 				}
 			}
-		}, isCheat: false, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, null, alwaysRefreshTabOptions: false, remoteCommand: true);
+		}, isCheat: false, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: false, null, alwaysRefreshTabOptions: false, remoteCommand: true);
 		new ConsoleCommand("puke", "empties your stomach of food", delegate
 		{
 			if ((bool)Player.m_localPlayer)
 			{
 				Player.m_localPlayer.ClearFood();
 			}
-		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: true, null, alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
+		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: true, hideBehindDevCommands: false, null, alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
 		new ConsoleCommand("tame", "tame all nearby tameable creatures", delegate
 		{
 			Tameable.TameAllInArea(Player.m_localPlayer.transform.position, 20f);
-		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, null, alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
+		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: false, null, alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
 		new ConsoleCommand("aggravate", "aggravated all nearby neutrals", delegate
 		{
 			BaseAI.AggravateAllInArea(Player.m_localPlayer.transform.position, 20f, BaseAI.AggravatedReason.Damage);
-		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, null, alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
+		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: false, null, alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
 		new ConsoleCommand("killall", "kill nearby creatures", delegate
 		{
 			List<Character> allCharacters = Character.GetAllCharacters();
 			int num2 = 0;
 			int num3 = 0;
-			foreach (Character item14 in allCharacters)
+			foreach (Character item15 in allCharacters)
 			{
-				if (!item14.IsPlayer() && !(item14.GetComponent<Piece>() != null))
+				if (ShouldKillAll(item15, killTamed: true))
 				{
-					item14.Damage(new HitData(1E+10f));
+					item15.Damage(new HitData(1E+10f));
 					num2++;
 				}
 			}
@@ -1184,31 +1319,31 @@
 				}
 			}
 			Player.m_localPlayer.Message(MessageHud.MessageType.TopLeft, string.Format("Killed {0} monsters{1}", num2, (num3 > 0) ? $" & {num3} spawners." : "."));
-		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, null, alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
+		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: false, null, alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
 		new ConsoleCommand("killenemycreatures", "kill nearby enemies", delegate
 		{
 			List<Character> allCharacters = Character.GetAllCharacters();
 			int num2 = 0;
-			foreach (Character item15 in allCharacters)
+			foreach (Character item16 in allCharacters)
 			{
-				if (ShouldKillAll(item15))
+				if (ShouldKillAll(item16))
 				{
-					item15.Damage(new HitData(1E+10f));
+					item16.Damage(new HitData(1E+10f));
 					num2++;
 				}
 			}
 			Player.m_localPlayer.Message(MessageHud.MessageType.TopLeft, $"Killed {num2} monsters.");
-		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, null, alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
+		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: false, null, alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
 		new ConsoleCommand("killenemies", "kill nearby enemies", delegate
 		{
 			List<Character> allCharacters = Character.GetAllCharacters();
 			int num2 = 0;
 			int num3 = 0;
-			foreach (Character item16 in allCharacters)
+			foreach (Character item17 in allCharacters)
 			{
-				if (ShouldKillAll(item16))
+				if (ShouldKillAll(item17))
 				{
-					item16.Damage(new HitData(1E+10f));
+					item17.Damage(new HitData(1E+10f));
 					num2++;
 				}
 			}
@@ -1223,16 +1358,16 @@
 				}
 			}
 			Player.m_localPlayer.Message(MessageHud.MessageType.TopLeft, string.Format("Killed {0} monsters{1}", num2, (num3 > 0) ? $" & {num3} spawners." : "."));
-		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, null, alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
+		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: false, null, alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
 		new ConsoleCommand("killtame", "kill nearby tame creatures.", delegate
 		{
 			List<Character> allCharacters = Character.GetAllCharacters();
 			int num2 = 0;
-			foreach (Character item17 in allCharacters)
+			foreach (Character item18 in allCharacters)
 			{
-				if (ShouldKillAll(item17))
+				if (ShouldKillAll(item18, killTamed: true))
 				{
-					item17.Damage(new HitData
+					item18.Damage(new HitData
 					{
 						m_damage = 
 						{
@@ -1243,7 +1378,7 @@
 				}
 			}
 			Player.m_localPlayer.Message(MessageHud.MessageType.TopLeft, "Killing all tame creatures:" + num2);
-		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, null, alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
+		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: false, null, alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
 		new ConsoleCommand("heal", "heal to full health & stamina", delegate
 		{
 			if (!(Player.m_localPlayer == null))
@@ -1252,14 +1387,14 @@
 				Player.m_localPlayer.AddStamina(Player.m_localPlayer.GetMaxStamina());
 				Player.m_localPlayer.AddEitr(Player.m_localPlayer.GetMaxEitr());
 			}
-		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: true, null, alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
+		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: true, hideBehindDevCommands: false, null, alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
 		new ConsoleCommand("adrenaline", "sets adrenaline level", delegate(ConsoleEventArgs args)
 		{
 			if (!(Player.m_localPlayer == null))
 			{
 				Player.m_localPlayer.AddAdrenaline(args.TryParameterFloat(1, 100f) - Player.m_localPlayer.GetAdrenaline());
 			}
-		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: true, null, alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
+		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: true, hideBehindDevCommands: false, null, alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
 		new ConsoleCommand("god", "invincible mode", delegate(ConsoleEventArgs args)
 		{
 			if (!(Player.m_localPlayer == null))
@@ -1268,7 +1403,7 @@
 				args.Context.AddString("God mode:" + Player.m_localPlayer.InGodMode());
 				Gogan.LogEvent("Cheat", "God", Player.m_localPlayer.InGodMode().ToString(), 0L);
 			}
-		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: true, null, alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
+		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: true, hideBehindDevCommands: false, null, alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
 		new ConsoleCommand("ghost", "", delegate(ConsoleEventArgs args)
 		{
 			if (!(Player.m_localPlayer == null))
@@ -1277,7 +1412,7 @@
 				args.Context.AddString("Ghost mode:" + Player.m_localPlayer.InGhostMode());
 				Gogan.LogEvent("Cheat", "Ghost", Player.m_localPlayer.InGhostMode().ToString(), 0L);
 			}
-		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: true, null, alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
+		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: true, hideBehindDevCommands: false, null, alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
 		new ConsoleCommand("nospawn", "toggles natural spawning of monsters", delegate(ConsoleEventArgs args)
 		{
 			SpawnSystem.m_nospawn = args.HasArgumentAnywhere("on") || (!args.HasArgumentAnywhere("off") && !SpawnSystem.m_nospawn);
@@ -1294,7 +1429,7 @@
 				Player.m_localPlayer.SetBeard(args[1]);
 			}
 			return true;
-		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, delegate
+		}, isCheat: false, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: true, delegate
 		{
 			List<string> list = new List<string>();
 			foreach (ItemDrop allItem in ObjectDB.instance.GetAllItems(ItemDrop.ItemData.ItemType.Customization, "Beard"))
@@ -1314,7 +1449,7 @@
 				Player.m_localPlayer.SetHair(args[1]);
 			}
 			return true;
-		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, delegate
+		}, isCheat: false, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: true, delegate
 		{
 			List<string> list = new List<string>();
 			foreach (ItemDrop allItem2 in ObjectDB.instance.GetAllItems(ItemDrop.ItemData.ItemType.Customization, "Hair"))
@@ -1334,7 +1469,7 @@
 				Player.m_localPlayer.SetPlayerModel(value);
 			}
 			return true;
-		}, isCheat: true);
+		}, isCheat: false, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: true);
 		new ConsoleCommand("tod", "-1 OR [0-1]", delegate(ConsoleEventArgs args)
 		{
 			if (EnvMan.instance == null || args.Length < 2 || !args.TryParameterFloat(1, out var value))
@@ -1355,15 +1490,22 @@
 		}, isCheat: true, isNetwork: false, onlyServer: true, isSecret: false, allowInDevBuild: true);
 		new ConsoleCommand("env", "[env] override environment", delegate(ConsoleEventArgs args)
 		{
-			if (EnvMan.instance == null || args.Length < 2)
+			if (EnvMan.instance == null)
 			{
 				return false;
 			}
+			if (args.Length == 1)
+			{
+				args.Context.AddString("Current environment: " + EnvMan.instance.GetCurrentEnvironment().m_name);
+				args.Context.AddString("Possible: " + string.Join(", ", EnvMan.instance.GetAvailableEnvironments(Player.m_localPlayer.GetCurrentBiomeData())));
+				args.Context.AddString("All: " + string.Join(", ", args.Commmand.GetTabOptions()));
+				return false;
+			}
 			string text = string.Join(" ", args.Args, 1, args.Args.Length - 1);
 			args.Context.AddString("Setting debug enviornment:" + text);
 			EnvMan.instance.m_debugEnv = text;
 			return true;
-		}, isCheat: true, isNetwork: false, onlyServer: true, isSecret: false, allowInDevBuild: true, delegate
+		}, isCheat: true, isNetwork: false, onlyServer: true, isSecret: false, allowInDevBuild: true, hideBehindDevCommands: false, delegate
 		{
 			List<string> list = new List<string>();
 			foreach (EnvSetup environment in EnvMan.instance.m_environments)
@@ -1372,6 +1514,42 @@
 			}
 			return list;
 		});
+		new ConsoleCommand("snow", "[amount] adds/removes amount of snowbuild on all nearby buildpieces", delegate(ConsoleEventArgs args)
+		{
+			if (EnvMan.instance == null)
+			{
+				return false;
+			}
+			if (args.TryParameterFloat(1, out var value))
+			{
+				WearNTear[] array = UnityEngine.Object.FindObjectsOfType<WearNTear>();
+				foreach (WearNTear wearNTear in array)
+				{
+					if (value < 0f || wearNTear.CanHaveSnow(forceCover: true))
+					{
+						wearNTear.m_snowBuildup = Math.Clamp(wearNTear.m_snowBuildup + value, 0f, 1f);
+					}
+					wearNTear.UpdateSnowVisual();
+				}
+			}
+			return true;
+		}, isCheat: true, isNetwork: false, onlyServer: true, isSecret: false, allowInDevBuild: true);
+		new ConsoleCommand("updatecover", "force update cover refresh", delegate(ConsoleEventArgs args)
+		{
+			if (EnvMan.instance == null)
+			{
+				return false;
+			}
+			if (args.TryParameterFloat(1, out var _))
+			{
+				WearNTear[] array = UnityEngine.Object.FindObjectsOfType<WearNTear>();
+				for (int i = 0; i < array.Length; i++)
+				{
+					array[i].UpdateCover(100f);
+				}
+			}
+			return true;
+		}, isCheat: false, isNetwork: false, onlyServer: true, isSecret: false, allowInDevBuild: true, hideBehindDevCommands: true);
 		new ConsoleCommand("resetenv", "disables environment override", delegate(ConsoleEventArgs args)
 		{
 			if (EnvMan.instance == null)
@@ -1382,6 +1560,41 @@
 			EnvMan.instance.m_debugEnv = "";
 			return true;
 		}, isCheat: true, isNetwork: false, onlyServer: true, isSecret: false, allowInDevBuild: true);
+		new ConsoleCommand("cinematic", "[name] play cinematic", (ConsoleEventArgs args) => (EnvMan.instance == null || args.Length < 2) ? ((object)false) : ((object)CinematicsManager.Play(string.Join(" ", args.Args, 1, args.Args.Length - 1))), isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: true, hideBehindDevCommands: false, delegate
+		{
+			List<string> list = new List<string>();
+			foreach (CinematicsManager.VideoEntry video in CinematicsManager.s_instance.m_videos)
+			{
+				list.Add(video.m_name);
+			}
+			return list;
+		});
+		new ConsoleCommand("inventorysize", "sets inventory size", delegate(ConsoleEventArgs args)
+		{
+			if (Player.m_localPlayer == null)
+			{
+				return false;
+			}
+			if (args.TryParameterInt(1, out var value))
+			{
+				Player.m_localPlayer.SetInventorySize(value);
+				return true;
+			}
+			return $"Inventory size: {Player.m_localPlayer.GetInventory().GetHeight()}";
+		}, isCheat: true, isNetwork: false, onlyServer: true, isSecret: false, allowInDevBuild: true);
+		new ConsoleCommand("cinematicsleep", "[name] play cinematic on next sleep for everyone", (ConsoleEventArgs args) => (EnvMan.instance == null || args.Length < 2) ? ((object)false) : ((object)CinematicsManager.SetDreamCinematic(string.Join(" ", args.Args, 1, args.Args.Length - 1))), isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: true, hideBehindDevCommands: false, delegate
+		{
+			List<string> list = new List<string>();
+			foreach (CinematicsManager.VideoEntry video2 in CinematicsManager.s_instance.m_videos)
+			{
+				list.Add(video2.m_name);
+			}
+			return list;
+		});
+		new ConsoleCommand("inventoryclean", "throws out any items in inventory on invalid positions", delegate
+		{
+			Player.m_localPlayer.DropInvalidItems();
+		});
 		new ConsoleCommand("wind", "[angle] [intensity]", delegate(ConsoleEventArgs args)
 		{
 			if (args.TryParameterFloat(1, out var value) && args.TryParameterFloat(2, out var value2))
@@ -1400,10 +1613,15 @@
 			args.Context.m_chatBuffer.Clear();
 			args.Context.UpdateChat();
 		});
+		new ConsoleCommand("clearpopups", "stops all current unlock UI messages", delegate
+		{
+			MessageHud.instance.ClearUnlockQueue();
+		});
 		new ConsoleCommand("filtercraft", "[name] filters crafting list to contain part of text", delegate(ConsoleEventArgs args)
 		{
 			if (args.Length <= 1)
 			{
+				args.Context.AddString("Cleared crafting filter.");
 				Player.s_FilterCraft.Clear();
 			}
 			else
@@ -1415,22 +1633,22 @@
 		{
 			Player.m_localPlayer.ClearHardDeath();
 			Player.m_localPlayer.GetSEMan().RemoveAllStatusEffects();
-		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: true, null, alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
+		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: true, hideBehindDevCommands: false, null, alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
 		new ConsoleCommand("addstatus", "[name] adds a status effect (ex: Rested, Burning, SoftDeath, Wet, etc)", delegate(ConsoleEventArgs args)
 		{
 			if (args.Length < 2)
 			{
 				return false;
 			}
-			Player.m_localPlayer.GetSEMan().AddStatusEffect(args[1].GetStableHashCode(), resetTime: true);
+			Player.m_localPlayer.GetSEMan().AddStatusEffect(args[1].GetStableHashCode(), resetTime: true, 0, 0f, -1);
 			return true;
-		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: true, delegate
+		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: true, hideBehindDevCommands: false, delegate
 		{
 			List<StatusEffect> statusEffects = ObjectDB.instance.m_StatusEffects;
 			List<string> list = new List<string>();
-			foreach (StatusEffect item18 in statusEffects)
+			foreach (StatusEffect item19 in statusEffects)
 			{
-				list.Add(item18.name);
+				list.Add(item19.name);
 			}
 			return list;
 		}, alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
@@ -1443,13 +1661,13 @@
 			Player.m_localPlayer.SetGuardianPower(args[1]);
 			Player.m_localPlayer.m_guardianPowerCooldown = 0f;
 			return true;
-		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: true, delegate
+		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: true, hideBehindDevCommands: false, delegate
 		{
 			List<StatusEffect> statusEffects = ObjectDB.instance.m_StatusEffects;
 			List<string> list = new List<string>();
-			foreach (StatusEffect item19 in statusEffects)
+			foreach (StatusEffect item20 in statusEffects)
 			{
-				list.Add(item19.name);
+				list.Add(item20.name);
 			}
 			return list;
 		}, alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
@@ -1506,17 +1724,90 @@
 			}
 			updateBinds();
 		});
+		new ConsoleCommand("height", "prints height from worldgen at current position", delegate(ConsoleEventArgs args)
+		{
+			if (Player.m_localPlayerExists)
+			{
+				args.Context.AddString($"{WorldGenerator.instance.GetHeight(Player.m_localPlayer.transform.position)}");
+			}
+		});
+		new ConsoleCommand("pevents", "Subcommands: [list/start/stop/here]", delegate(ConsoleEventArgs args)
+		{
+			if (args.Length < 2)
+			{
+				args.Context.AddString("Usage: pevents [list/start/stop]");
+			}
+			else if (args[1].ToLower() == "list")
+			{
+				PrintEventList();
+			}
+			else if (args[1].ToLower() == "start")
+			{
+				if (args.Length < 3)
+				{
+					args.Context.AddString("Usage: pevents start [eventname]");
+				}
+				else
+				{
+					string text = args[2].ToLower();
+					PersistentEventSystem.instance.TriggerEvent(text);
+					args.Context.AddString("Attempted to start event " + text + "...");
+					PrintEventList();
+				}
+			}
+			else if (args[1].ToLower() == "stop")
+			{
+				if (args.Length < 3)
+				{
+					args.Context.AddString("Usage: pevents stop [eventname]");
+				}
+				else
+				{
+					string text2 = args[2].ToLower();
+					PersistentEventSystem.instance.StopEvent(text2, Player.m_localPlayer.transform.position);
+					args.Context.AddString("Attempted to stop event " + text2 + " closest to player...");
+					PrintEventList();
+				}
+			}
+			else if (args[1].ToLower() == "here")
+			{
+				PersistentEventSystem.PersistentEvent activeEvent = PersistentEventSystem.instance.GetActiveEvent(Player.m_localPlayer.transform.position);
+				if (activeEvent == null)
+				{
+					args.Context.AddString("No event at player position");
+				}
+				else
+				{
+					args.Context.AddString("Event at player is " + activeEvent.internalName);
+				}
+				PrintEventList();
+			}
+			void PrintEventList()
+			{
+				PersistentEventSystem.ActiveEventsList activePersistentEvents = PersistentEventSystem.instance.m_activePersistentEvents;
+				if (activePersistentEvents.list.Count == 0)
+				{
+					args.Context.AddString("No active persistent events found.");
+					return;
+				}
+				args.Context.AddString("Current events:");
+				foreach (PersistentEventSystem.ActivePersistentEvent item21 in activePersistentEvents.list)
+				{
+					args.Context.AddString($"{item21.eventId}: {item21.internalName} | Position: {item21.position}");
+				}
+			}
+		});
 		new ConsoleCommand("tombstone", "[name] creates a tombstone with given name", delegate(ConsoleEventArgs args)
 		{
 			GameObject obj = UnityEngine.Object.Instantiate(Player.m_localPlayer.m_tombstone, Player.m_localPlayer.GetCenterPoint(), Player.m_localPlayer.transform.rotation);
 			Container component = obj.GetComponent<Container>();
 			ItemDrop coinPrefab = StoreGui.instance.m_coinPrefab;
-			component.GetInventory().AddItem(coinPrefab.gameObject.name, 1, coinPrefab.m_itemData.m_quality, coinPrefab.m_itemData.m_variant, 0L, "", pickedUp: true);
+			component.GetInventory().AddItem(coinPrefab.gameObject.name, 1, coinPrefab.m_itemData.m_quality, coinPrefab.m_itemData.m_variant, 0L, "", cheated: true, pickedUp: true);
 			TombStone component2 = obj.GetComponent<TombStone>();
 			PlayerProfile playerProfile = Game.instance.GetPlayerProfile();
 			string ownerName = ((args.Args.Length >= 2) ? args.Args[1] : playerProfile.GetName());
 			component2.Setup(ownerName, playerProfile.GetPlayerID());
-		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, null, alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
+		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: false, null, alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
 		new ConsoleCommand("test", "[key] [value] set test string, with optional value. set empty existing key to remove", delegate(ConsoleEventArgs args)
 		{
 			if (args.Length < 2)
@@ -1554,14 +1845,14 @@
 				break;
 			}
 			return true;
-		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: true);
-		new ConsoleCommand("forcedelete", "[radius] [*name] force remove all objects within given radius. If name is entered, only deletes items with matching names. Caution! Use at your own risk. Make backups! Radius default: 5, max: 50.", delegate(ConsoleEventArgs args)
+		}, isCheat: false, isNetwork: false, onlyServer: false, isSecret: true);
+		new ConsoleCommand("forcedelete", "[radius] [*name] force remove all objects within given radius. If name is entered, only deletes items with matching names. Caution! Use at your own risk. Make backups! Radius default: 5, max: 150.", delegate(ConsoleEventArgs args)
 		{
 			if (Player.m_localPlayer == null)
 			{
 				return false;
 			}
-			float num2 = Math.Min(50f, args.TryParameterFloat(1, 5f));
+			float num2 = Math.Min(150f, args.TryParameterFloat(1, 5f));
 			UnityEngine.Object[] array = UnityEngine.Object.FindObjectsOfType(typeof(GameObject));
 			for (int i = 0; i < array.Length; i++)
 			{
@@ -1585,7 +1876,7 @@
 				}
 			}
 			return true;
-		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, null, alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
+		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: false, null, alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
 		new ConsoleCommand("stopfire", "Puts out all spreading fires and smoke", delegate
 		{
 			if (Player.m_localPlayer == null)
@@ -1595,7 +1886,7 @@
 			RemoveObj(UnityEngine.Object.FindObjectsOfType(typeof(Fire)));
 			RemoveObj(UnityEngine.Object.FindObjectsOfType(typeof(Smoke)));
 			return true;
-		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, null, alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
+		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: false, null, alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
 		new ConsoleCommand("stopsmoke", "Puts out all spreading fires", delegate
 		{
 			if (Player.m_localPlayer == null)
@@ -1618,7 +1909,21 @@
 				}
 			}
 			return true;
-		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, null, alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
+		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: false, null, alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
+		new ConsoleCommand("repairall", "repairs all equipment", delegate
+		{
+			if (Player.m_localPlayer == null)
+			{
+				return false;
+			}
+			List<ItemDrop.ItemData> list = new List<ItemDrop.ItemData>();
+			Player.m_localPlayer.GetInventory().GetWornItems(list);
+			foreach (ItemDrop.ItemData item22 in list)
+			{
+				item22.m_durability = item22.GetMaxDurability();
+			}
+			return true;
+		}, isCheat: true);
 		new ConsoleCommand("printseeds", "print seeds of loaded dungeons", delegate(ConsoleEventArgs args)
 		{
 			if (Player.m_localPlayer == null)
@@ -1673,7 +1978,7 @@
 				}
 				args.Context?.AddString("Portals " + (globalKey ? "enabled" : "disabled"));
 			}
-		}, isCheat: false, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, null, alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
+		}, isCheat: false, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: false, null, alwaysRefreshTabOptions: false, remoteCommand: false, onlyAdmin: true);
 		new ConsoleCommand("resetspawn", "resets spawn location", delegate(ConsoleEventArgs args)
 		{
 			if (!Game.instance)
@@ -1753,21 +2058,213 @@
 				Emote.DoEmote(emote);
 			});
 		}
-		new ConsoleCommand("resetplayerprefs", "Resets any saved settings and variables (not the save game)", delegate(ConsoleEventArgs args)
+		new ConsoleCommand("resetbuildui", "Resets build ui save data that stores recent and favorites", delegate(ConsoleEventArgs args)
 		{
-			PlatformPrefs.DeleteAll();
-			args.Context?.AddString("Reset saved player preferences");
+			if ((bool)Hud.instance)
+			{
+				Hud.instance.m_buildUi.ResetSaveData();
+				args.Context?.AddString("Reset build UI save data");
+			}
+			else
+			{
+				args.Context?.AddString("The build UI can only be reset after having entered a world");
+			}
 		}, isCheat: false, isNetwork: false, onlyServer: false, isSecret: true, allowInDevBuild: true);
-		static bool ShouldKillAll(Character c)
+		new ConsoleCommand("biomeinfo", "info about current biome sector", delegate(ConsoleEventArgs args)
 		{
+			BiomeSector biomeSector = WorldGenerator.instance.GetBiomeSector(Player.m_localPlayer.transform.position);
+			args.Context.AddString(biomeSector.ToString());
+		}, isCheat: true);
+		new ConsoleCommand("altbioms", "list of biomes with modifiers", delegate(ConsoleEventArgs args)
+		{
+			Dictionary<Heightmap.Biome, int> dictionary = new Dictionary<Heightmap.Biome, int>();
+			AltBiomeWorldData biomeData = WorldGenerator.instance.m_world.m_biomeData;
+			if (biomeData == null)
+			{
+				args.Context.AddString("Minimap & BiomeData not generated.");
+				return;
+			}
+			args.Context.AddString("Modifiers will only appear in unexplored zones.");
+			args.Context.AddString($"AltBiome modifiers total: {AltBiomeList.m_altBiomes.Count}");
+			dictionary.Clear();
+			foreach (KeyValuePair<Heightmap.Biome, int> item23 in dictionary)
+			{
+				foreach (AltBiome altBiome in AltBiomeList.m_altBiomes)
+				{
+					if (altBiome.m_biome.HasFlag(item23.Key))
+					{
+						dictionary[item23.Key] = item23.Value + 1;
+					}
+				}
+			}
+			foreach (KeyValuePair<Heightmap.Biome, int> item24 in dictionary)
+			{
+				args.Context.AddString($"   {item24.Key}: {item24.Value}\n");
+			}
+			if (biomeData.groupTex != null)
+			{
+				foreach (KeyValuePair<Heightmap.Biome, BiomeTypeInfo> biome2 in biomeData.Biomes)
+				{
+					args.Context.AddString($"{biome2.Value.Biome}: {biome2.Value.Sectors.Count} Modified: {biome2.Value.Sectors.Count((BiomeSector x) => x.AltBiomes.Count > 0)}");
+				}
+			}
+			args.Context.AddString("BiomeTypeInfo:");
+			foreach (KeyValuePair<Heightmap.Biome, BiomeTypeInfo> biome3 in WorldGenerator.instance.m_world.m_biomeData.Biomes)
+			{
+				foreach (BiomeSector sector in biome3.Value.Sectors)
+				{
+					if (sector.AltBiomes.Count > 0)
+					{
+						args.Context.AddString(sector.ToString());
+					}
+				}
+			}
+		}, isCheat: true);
+		new ConsoleCommand("findbiome", "marks center of all biomes on map of type or containing modifier with search string", delegate(ConsoleEventArgs args)
+		{
+			if (args.Length < 2)
+			{
+				return false;
+			}
+			string text = args[1].ToLower();
+			List<BiomeSector> list = new List<BiomeSector>();
+			foreach (BiomeSector sector2 in ZNet.instance.GetWorld().m_biomeData.Sectors)
+			{
+				if (sector2.Biome.ToString().ToLower().Contains(text) || sector2.GetName().ToLower().Contains(text) || Localization.instance.Localize(sector2.GetName()).ToLower().Contains(text))
+				{
+					list.Add(sector2);
+				}
+				else
+				{
+					foreach (AltBiome altBiome2 in sector2.AltBiomes)
+					{
+						if (altBiome2.m_name.ToLower().Contains(text) || altBiome2.m_namePrefix.Contains(text) || altBiome2.m_nameSuffix.Contains(text) || altBiome2.m_nameOverride.Contains(text))
+						{
+							list.Add(sector2);
+						}
+					}
+				}
+			}
+			list.Sort((BiomeSector a, BiomeSector b) => Vector3.Distance(a.Center, Player.m_localPlayer.transform.position).CompareTo(Vector3.Distance(b.Center, Player.m_localPlayer.transform.position)));
+			foreach (BiomeSector item25 in list)
+			{
+				args.Context.AddString($"   {item25}");
+			}
+			foreach (Minimap.PinData findPin2 in args.Context.m_findPins)
+			{
+				Minimap.instance.RemovePin(findPin2);
+			}
+			args.Context.m_findPins.Clear();
+			if (list.Count > 0)
+			{
+				Chat.instance.SendPing(new Vector3(list[0].Center.x, 0f, list[0].Center.y));
+				for (int num2 = 0; num2 < list.Count; num2++)
+				{
+					args.Context.m_findPins.Add(Minimap.instance.AddPin(new Vector3(list[num2].Center.x, 0f, list[num2].Center.y), Minimap.PinType.Icon2, list[num2].GetName(debug: true), save: false, isChecked: true, Player.m_localPlayer.GetPlayerID()));
+				}
+			}
+			args.Context.AddString($"Found {list.Count} biomes containing '{text}'");
+			return true;
+		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: false, delegate
+		{
+			List<string> list = new List<string>();
+			list.AddRange(Enum.GetNames(typeof(Heightmap.Biome)));
+			foreach (AltBiome altBiome3 in AltBiomeList.m_altBiomes)
+			{
+				if (altBiome3.m_name.Length > 0)
+				{
+					list.Add(altBiome3.m_name);
+				}
+				if (altBiome3.m_namePrefix.Length > 0)
+				{
+					list.Add(altBiome3.m_namePrefix);
+				}
+				if (altBiome3.m_nameSuffix.Length > 0)
+				{
+					list.Add(altBiome3.m_nameSuffix);
+				}
+				if (altBiome3.m_nameOverride.Length > 0)
+				{
+					list.Add(altBiome3.m_nameOverride);
+				}
+			}
+			return list;
+		});
+		new ConsoleCommand("findbiometp", "finds the closest biome matching your search and teleports you to it", delegate(ConsoleEventArgs args)
+		{
+			if (args.Length < 2)
+			{
+				return false;
+			}
+			string text = args[1].ToLower();
+			List<BiomeSector> list = new List<BiomeSector>();
+			foreach (BiomeSector sector3 in ZNet.instance.GetWorld().m_biomeData.Sectors)
+			{
+				if (sector3.Biome.ToString().ToLower().Contains(text) || sector3.GetName().ToLower().Contains(text) || Localization.instance.Localize(sector3.GetName()).ToLower().Contains(text))
+				{
+					list.Add(sector3);
+				}
+				else
+				{
+					foreach (AltBiome altBiome4 in sector3.AltBiomes)
+					{
+						if (altBiome4.m_name.ToLower().Contains(text) || altBiome4.m_namePrefix.Contains(text) || altBiome4.m_nameSuffix.Contains(text) || altBiome4.m_nameOverride.Contains(text))
+						{
+							list.Add(sector3);
+						}
+					}
+				}
+			}
+			list.Sort((BiomeSector a, BiomeSector b) => Vector3.Distance(a.Center, Player.m_localPlayer.transform.position).CompareTo(Vector3.Distance(b.Center, Player.m_localPlayer.transform.position)));
+			if (list.Count == 0)
+			{
+				args.Context.AddString($"Found {list.Count} biomes containing '{text}'");
+			}
+			else
+			{
+				Player.m_localPlayer.TeleportTo(new Vector3(list[0].Center.x, 0f, list[0].Center.y), Player.m_localPlayer.transform.rotation, distantTeleport: true);
+			}
+			return true;
+		}, isCheat: true, isNetwork: false, onlyServer: false, isSecret: false, allowInDevBuild: false, hideBehindDevCommands: false, delegate
+		{
+			List<string> list = new List<string>();
+			list.AddRange(Enum.GetNames(typeof(Heightmap.Biome)));
+			foreach (AltBiome altBiome5 in AltBiomeList.m_altBiomes)
+			{
+				if (altBiome5.m_name.Length > 0)
+				{
+					list.Add(altBiome5.m_name);
+				}
+				if (altBiome5.m_namePrefix.Length > 0)
+				{
+					list.Add(altBiome5.m_namePrefix);
+				}
+				if (altBiome5.m_nameSuffix.Length > 0)
+				{
+					list.Add(altBiome5.m_nameSuffix);
+				}
+				if (altBiome5.m_nameOverride.Length > 0)
+				{
+					list.Add(altBiome5.m_nameOverride);
+				}
+			}
+			return list;
+		});
+		static bool ShouldKillAll(Character c, bool killTamed = false)
+		{
 			if (c.IsPlayer() || c.GetComponent<Piece>() != null)
 			{
 				return false;
 			}
-			if (c.IsTamed())
+			if (c.IsTamed() && !killTamed)
 			{
 				return false;
 			}
+			if ((bool)Player.m_localPlayer && Vector3.Distance(Player.m_localPlayer.transform.position, c.transform.position) > 1000f)
+			{
+				ZLog.Log("Tried to kill " + c.m_name + ", but they were very far away. Ignored!");
+				return false;
+			}
 			return true;
 		}
 		void count(string key, int level, int increment = 1)
@@ -1810,9 +2307,9 @@
 			while (!ZDOMan.instance.GetAllZDOsWithPrefabIterative(q, list2, ref index))
 			{
 			}
-			foreach (ZDO item20 in list2)
+			foreach (ZDO item26 in list2)
 			{
-				list.Add(new Tuple<object, Vector3>(item20, item20.GetPosition()));
+				list.Add(new Tuple<object, Vector3>(item26, item26.GetPosition()));
 			}
 			return list;
 		}
```

### Valkyrie

**`Valkyrie.DropPlayer`** - Game.SkipIntro calls it on m_instance and it un-intros Player.m_localPlayer unguarded

```diff
--- C:/Users/donfr/valheim-shadows/src/baseline-client/assembly_valheim/Valkyrie.cs
+++ C:/Users/donfr/valheim-shadows/src/client-1.0.7/assembly_valheim/Valkyrie.cs
@@ -10,6 +10,7 @@
 		Player.m_localPlayer.transform.rotation = Quaternion.LookRotation(forward);
 		Player.m_localPlayer.SetIntro(intro: false);
 		m_animator.SetBool("dropped", value: true);
+		StartCoroutine(WaitForRespawn());
 		if (destroy)
 		{
 			m_nview.Destroy();
```

**`Valkyrie.UpdateValkyrie`** - the flight maths Client/CargoFlight.cs keeps: 25 m look-ahead, banked turn, 0.5 m arrival

```diff
--- C:/Users/donfr/valheim-shadows/src/baseline-client/assembly_valheim/Valkyrie.cs
+++ C:/Users/donfr/valheim-shadows/src/client-1.0.7/assembly_valheim/Valkyrie.cs
@@ -7,6 +7,11 @@
 		{
 			return;
 		}
+		if (!m_notifiedMultiplayerStart && !BlockCheckHandler.m_dialogOpen)
+		{
+			m_notifiedMultiplayerStart = true;
+			ZNet.instance.SetMultiplayerUsageStart();
+		}
 		Vector3 vector = (m_droppedPlayer ? m_flyAwayPoint : ((!m_descent) ? m_descentStart : m_targetPoint));
 		if (Utils.DistanceXZ(vector, base.transform.position) < 0.5f)
 		{
```

### ZDO

**`ZDO.IsValid`** - Spawner.Tick, Reclaim, CargoFlight.Drop

```diff
--- C:/Users/donfr/valheim-shadows/src/baseline-client/assembly_valheim/ZDO.cs
+++ C:/Users/donfr/valheim-shadows/src/client-1.0.7/assembly_valheim/ZDO.cs
@@ -2,5 +2,5 @@
 
 	public bool IsValid()
 	{
-		return Valid;
+		return m_prefab != -1;
 	}
```

### ZDOMan

**`ZDOMan.CreateNewZDO`** - Spawner.Author; the hash is used ONLY for the portal list

```diff
--- C:/Users/donfr/valheim-shadows/src/baseline-client/assembly_valheim/ZDOMan.cs
+++ C:/Users/donfr/valheim-shadows/src/client-1.0.7/assembly_valheim/ZDOMan.cs
@@ -4,12 +4,9 @@
 	{
 		ZDO zDO = ZDOPool.Create(uid, position);
 		zDO.SetOwnerInternal(m_sessionID);
+		int prefabHash = ((prefabHashIn != 0) ? prefabHashIn : zDO.GetPrefab());
+		AddIfPortal(zDO, prefabHash);
 		m_objectsByID.Add(uid, zDO);
-		int item = ((prefabHashIn != 0) ? prefabHashIn : zDO.GetPrefab());
-		if (Game.instance.PortalPrefabHash.Contains(item))
-		{
-			m_portalObjects.Add(zDO);
-		}
 		return zDO;
 	}
 
```

### ZNet

**`ZNet.Awake`** - ServerSync RegisterRPCPatch

```diff
--- C:/Users/donfr/valheim-shadows/src/baseline-client/assembly_valheim/ZNet.cs
+++ C:/Users/donfr/valheim-shadows/src/client-1.0.7/assembly_valheim/ZNet.cs
@@ -2,12 +2,15 @@
 
 	private void Awake()
 	{
+		ZDOID.m_loadID = 0u;
 		m_instance = this;
 		m_loadError = false;
 		m_routedRpc = new ZRoutedRpc(m_isServer);
-		m_zdoMan = new ZDOMan(m_zdoSectorsWidth);
+		m_zdoMan = new ZDOMan(512);
 		m_passwordDialog.gameObject.SetActive(value: false);
 		m_connectingDialog.gameObject.SetActive(value: false);
+		GraphicsSettingsManager.GraphicsSettingsChanged += SimulationDistanceServerHandshake;
+		SimulationDistanceServerHandshake();
 		WorldGenerator.Deitialize();
 		if (!SteamManager.Initialize())
 		{
@@ -29,7 +32,7 @@
 				m_bannedList = new SyncedList(new FileHelpers.FileLocation(FileHelpers.FileSource.Local, Utils.GetSaveDataPath(FileHelpers.FileSource.Local) + "/bannedlist.txt"), "List banned players ID  ONE per line");
 				m_permittedList = new SyncedList(new FileHelpers.FileLocation(FileHelpers.FileSource.Local, Utils.GetSaveDataPath(FileHelpers.FileSource.Local) + "/permittedlist.txt"), "List permitted players ID ONE per line");
 			}
-			else if (FileHelpers.CloudStorageEnabled)
+			else if (FileHelpers.CloudStorageSupportedAndEnabled)
 			{
 				m_adminList = new SyncedList(new FileHelpers.FileLocation(FileHelpers.FileSource.Cloud, Utils.GetSaveDataPath(FileHelpers.FileSource.Cloud) + "/adminlist.txt"), "List admin players ID  ONE per line");
 				m_bannedList = new SyncedList(new FileHelpers.FileLocation(FileHelpers.FileSource.Cloud, Utils.GetSaveDataPath(FileHelpers.FileSource.Cloud) + "/bannedlist.txt"), "List banned players ID  ONE per line");
@@ -53,6 +56,7 @@
 		if (IsServer())
 		{
 			SendPlayerList();
+			SendHistoricalPlayerList();
 		}
 		if (!IsDedicated())
 		{
```

**`ZNet.ListContainsId`** - ServerSync; private

```diff
--- C:/Users/donfr/valheim-shadows/src/baseline-client/assembly_valheim/ZNet.cs
+++ C:/Users/donfr/valheim-shadows/src/client-1.0.7/assembly_valheim/ZNet.cs
@@ -6,13 +6,12 @@
 		{
 			platform = new PlatformUserID(m_steamPlatform, idString);
 		}
-		if (platform.m_platform == m_steamPlatform)
+		bool flag = false;
+		flag = ((!(platform.m_platform == m_steamPlatform)) ? list.Contains(platform.ToString()) : (list.Contains(platform.ToString()) || list.Contains(platform.m_userID.ToString())));
+		PlatformUserID platformUserID = PlatformUserID.FilterPlatformUserID(platform);
+		if (platformUserID != platform)
 		{
-			if (!list.Contains(platform.ToString()))
-			{
-				return list.Contains(platform.m_userID.ToString());
-			}
-			return true;
+			flag = list.Contains(platformUserID.ToString());
 		}
-		return list.Contains(platform.ToString());
+		return flag;
 	}
```

**`ZNet.OnNewConnection`** - ServerSync

```diff
--- C:/Users/donfr/valheim-shadows/src/baseline-client/assembly_valheim/ZNet.cs
+++ C:/Users/donfr/valheim-shadows/src/client-1.0.7/assembly_valheim/ZNet.cs
@@ -6,13 +6,15 @@
 		peer.m_rpc.Register<ZPackage>("PeerInfo", RPC_PeerInfo);
 		peer.m_rpc.Register("Disconnect", RPC_Disconnect);
 		peer.m_rpc.Register("SavePlayerProfile", RPC_SavePlayerProfile);
+		peer.m_rpc.Register<ZPackage>("RPC_RequestValidSimulationDistance", RPC_RequestValidSimulationDistance);
+		peer.m_rpc.Register<ZPackage>("RPC_ValidatedSimulationDistance", RPC_ValidatedSimulationDistance);
 		if (m_isServer)
 		{
-			peer.m_rpc.Register("ServerHandshake", RPC_ServerHandshake);
+			peer.m_rpc.Register<string>("ServerHandshake", RPC_ServerHandshake);
 			return;
 		}
 		peer.m_rpc.Register("Kicked", RPC_Kicked);
 		peer.m_rpc.Register<int>("Error", RPC_Error);
 		peer.m_rpc.Register<bool, string>("ClientHandshake", RPC_ClientHandshake);
-		peer.m_rpc.Invoke("ServerHandshake");
+		peer.m_rpc.Invoke("ServerHandshake", m_inviteSecretKey);
 	}
```

**`ZNet.RPC_PeerInfo`** - ServerSync's buffering socket around the handshake

```diff
--- C:/Users/donfr/valheim-shadows/src/baseline-client/assembly_valheim/ZNet.cs
+++ C:/Users/donfr/valheim-shadows/src/client-1.0.7/assembly_valheim/ZNet.cs
@@ -16,8 +16,8 @@
 		}
 		string name = peer.m_socket.GetEndPointString();
 		string hostName = peer.m_socket.GetHostName();
-		ZLog.Log("Network version check, their:" + num + ", mine:" + 36u);
-		if (num != 36)
+		ZLog.Log("Network version check, their:" + num + ", mine:" + 39u);
+		if (num != 39)
 		{
 			if (m_isServer)
 			{
@@ -34,7 +34,7 @@
 				" has incompatible version, mine:",
 				Version.CurrentVersion.ToString(),
 				" (network version ",
-				36u.ToString(),
+				39u.ToString(),
 				")   remote ",
 				null,
 				null,
@@ -51,6 +51,8 @@
 		}
 		Vector3 refPos = pkg.ReadVector3();
 		string text = pkg.ReadString();
+		string playfabId = pkg.ReadString();
+		SimulationDistance simulationDistance = SimulationDistance.Deserialize(ref pkg);
 		if (m_isServer)
 		{
 			if (!IsAllowed(hostName, text))
@@ -59,7 +61,12 @@
 				ZLog.Log("Player " + text + " : " + hostName + " is blacklisted or not in whitelist.");
 				return;
 			}
+			ZPackage pkg2 = new ZPackage();
+			simulationDistance.Serialize(ref pkg2);
+			pkg2.SetPos(0);
+			RPC_RequestValidSimulationDistance(rpc, pkg2);
 			string text2 = pkg.ReadString();
+			string text3 = pkg.ReadString();
 			if (m_onlineBackend == OnlineBackendType.Steamworks)
 			{
 				ZSteamSocket zSteamSocket = peer.m_socket as ZSteamSocket;
@@ -73,19 +80,25 @@
 			}
 			if (m_onlineBackend == OnlineBackendType.PlayFab)
 			{
-				PlatformUserID platformUserId = new PlatformUserID(peer.m_socket.GetHostName());
-				if (!platformUserId.IsValid)
+				PlatformUserID platformUserID = new PlatformUserID(peer.m_socket.GetHostName());
+				if (!platformUserID.IsValid)
 				{
 					ZLog.LogError("Failed to parse peer id! Using blank ID with unknown platform.");
-					platformUserId = PlatformUserID.None;
+					platformUserID = PlatformUserID.None;
 				}
-				if (PlatformManager.DistributionPlatform.PrivilegeProvider.CheckPrivilege(Privilege.CrossPlatformMultiplayer) != PrivilegeResult.Granted && PlatformManager.DistributionPlatform.Platform != platformUserId.m_platform)
+				else if (BlockList.Instance.Contains(platformUserID) || (PlatformManager.DistributionPlatform.Platform == "PlayStation" && RelationsManager.IsBlocked(platformUserID)))
 				{
+					ZLog.Log($"The player with the ID {platformUserID} is blocked by the server owner (you), disconnecting them!");
+					rpc.Invoke("Error", 8);
+					return;
+				}
+				if (!PrivilegeManager.CrossplayAllowed() && PlatformManager.DistributionPlatform.Platform != platformUserID.m_platform)
+				{
 					rpc.Invoke("Error", 10);
-					ZLog.Log("Peer diconnected due to server platform privileges disallowing crossplay. Server platform: " + PlatformManager.DistributionPlatform.Platform.ToString() + "   Peer platform: " + platformUserId.m_platform.ToString());
+					ZLog.Log("Peer diconnected due to server platform privileges disallowing crossplay. Server platform: " + PlatformManager.DistributionPlatform.Platform.ToString() + "   Peer platform: " + platformUserID.m_platform.ToString());
 					return;
 				}
-				PlayFabManager.CheckIfUserAuthenticated((peer.m_socket as ZPlayFabSocket).m_remotePlayerId, platformUserId, delegate(bool isAuthenticated)
+				PlayFabManager.CheckIfUserAuthenticated((peer.m_socket as ZPlayFabSocket).m_remotePlayerId, platformUserID, delegate(bool isAuthenticated)
 				{
 					if (!isAuthenticated)
 					{
@@ -102,38 +115,46 @@
 			}
 			if (m_serverPassword != text2)
 			{
+				if (!string.IsNullOrEmpty(text3))
+				{
+					IDistributionPlatform distributionPlatform = PlatformManager.DistributionPlatform;
+					if (distributionPlatform != null && distributionPlatform.MatchmakingProvider?.SecretKeyOk(text3) == true)
+					{
+						ZLog.Log("Using secret key from invite to peer " + name + " to bypass password check!");
+						goto IL_04c8;
+					}
+				}
 				rpc.Invoke("Error", 6);
 				ZLog.Log("Peer " + name + " has wrong password");
 				return;
 			}
-			if (IsConnected(uid))
-			{
-				rpc.Invoke("Error", 7);
-				ZLog.Log("Already connected to peer with UID:" + uid + "  " + name);
-				return;
-			}
-		}
-		else
-		{
-			m_world = new World();
-			m_world.m_name = pkg.ReadString();
-			m_world.m_seed = pkg.ReadInt();
-			m_world.m_seedName = pkg.ReadString();
-			m_world.m_uid = pkg.ReadLong();
-			m_world.m_worldGenVersion = pkg.ReadInt();
-			WorldGenerator.Initialize(m_world);
-			m_netTime = pkg.ReadDouble();
+			goto IL_04c8;
 		}
+		m_world = new World();
+		m_world.m_name = pkg.ReadString();
+		m_world.m_seed = pkg.ReadInt();
+		m_world.m_seedName = pkg.ReadString();
+		m_world.m_uid = pkg.ReadLong();
+		m_world.m_worldGenVersion = pkg.ReadInt();
+		WorldGenerator.Initialize(m_world);
+		AltBiomeWorldData.VerifyBiomeData(m_world);
+		m_netTime = pkg.ReadDouble();
+		goto IL_058f;
+		IL_058f:
 		peer.m_refPos = refPos;
 		peer.m_uid = uid;
 		peer.m_playerName = text;
+		peer.m_playfabId = playfabId;
 		rpc.Register<ZPackage>("ServerSyncedPlayerData", RPC_ServerSyncedPlayerData);
 		rpc.Register<ZPackage>("PlayerList", RPC_PlayerList);
+		rpc.Register<ZPackage>("HistoricalPlayerList", RPC_HistoricalPlayerList);
 		rpc.Register<ZPackage>("AdminList", RPC_AdminList);
 		rpc.Register<string>("RemotePrint", RPC_RemotePrint);
 		if (m_isServer)
 		{
 			rpc.Register<ZDOID>("CharacterID", RPC_CharacterID);
+			rpc.Register<long>("PlayerID", RPC_PlayerID);
+			rpc.Register<bool>("RPC_PeerRealtimeMultiplaying", RPC_PeerRealtimeMultiplaying);
 			rpc.Register<string>("Kick", RPC_Kick);
 			rpc.Register<string>("Ban", RPC_Ban);
 			rpc.Register<string>("Unban", RPC_Unban);
@@ -150,6 +171,7 @@
 			SendPeerInfo(rpc);
 			peer.m_socket.VersionMatch();
 			SendPlayerList();
+			SendHistoricalPlayerList();
 			SendAdminList();
 		}
 		else
@@ -159,4 +181,13 @@
 		}
 		m_zdoMan.AddPeer(peer);
 		m_routedRpc.AddPeer(peer);
+		return;
+		IL_04c8:
+		if (IsConnected(uid))
+		{
+			rpc.Invoke("Error", 7);
+			ZLog.Log("Already connected to peer with UID:" + uid + "  " + name);
+			return;
+		}
+		goto IL_058f;
 	}
```

### ZNetScene

**`ZNetScene.CreateObject`** - parks the ZDO in ZNetView.m_initZDO before Awake

```diff
--- C:/Users/donfr/valheim-shadows/src/baseline-client/assembly_valheim/ZNetScene.cs
+++ C:/Users/donfr/valheim-shadows/src/client-1.0.7/assembly_valheim/ZNetScene.cs
@@ -10,6 +10,7 @@
 		GameObject prefab2 = GetPrefab(prefab);
 		if (prefab2 == null)
 		{
+			ZLog.LogWarning("Missing prefab hash: " + prefab.StringFromHash());
 			return null;
 		}
 		Vector3 position = zdo.GetPosition();
```

**`ZNetScene.CreateObjectsSorted`** - the destroy-unresolvable-prefab branch that the server's pinned position keeps away from us

```diff
--- C:/Users/donfr/valheim-shadows/src/baseline-client/assembly_valheim/ZNetScene.cs
+++ C:/Users/donfr/valheim-shadows/src/client-1.0.7/assembly_valheim/ZNetScene.cs
@@ -27,7 +27,7 @@
 			if (CreateObject(item) != null)
 			{
 				created++;
-				if (created > num)
+				if (created >= num)
 				{
 					break;
 				}
@@ -36,7 +36,7 @@
 			{
 				item.SetOwner(ZDOMan.GetSessionID());
 				ZDOID uid = item.m_uid;
-				ZLog.Log("Destroyed invalid predab ZDO:" + uid.ToString());
+				ZLog.Log("Destroyed invalid prefab ZDO:" + uid.ToString());
 				ZDOMan.instance.DestroyZDO(item);
 			}
 		}
```

### ZNetView

**`ZNetView.Awake`** - THE authored-ZDO fact: the init-ZDO branch re-applies Type and Distant but NOT Persistent

```diff
--- C:/Users/donfr/valheim-shadows/src/baseline-client/assembly_valheim/ZNetView.cs
+++ C:/Users/donfr/valheim-shadows/src/client-1.0.7/assembly_valheim/ZNetView.cs
@@ -47,16 +47,16 @@
 		}
 		else
 		{
-			string prefabName = GetPrefabName();
-			m_zdo = ZDOMan.instance.CreateNewZDO(base.transform.position, prefabName.GetStableHashCode());
+			int stableHashCode = GetPrefabName().GetStableHashCode();
+			m_zdo = ZDOMan.instance.CreateNewZDO(base.transform.position, stableHashCode);
 			m_zdo.Persistent = m_persistent;
 			m_zdo.Type = m_type;
 			m_zdo.Distant = m_distant;
-			m_zdo.SetPrefab(prefabName.GetStableHashCode());
+			m_zdo.SetPrefab(stableHashCode);
 			m_zdo.SetRotation(base.transform.rotation);
 			if (m_syncInitialScale)
 			{
-				SyncScale(skipOne: true);
+				SyncScale();
 			}
 			if (m_ghostInit)
 			{
```

### ZSyncAnimation

**`ZSyncAnimation.Awake`** - root-scoped GetComponentInChildren<Animator>; BodyLoader's appended-last defence

```diff
--- C:/Users/donfr/valheim-shadows/src/baseline-client/assembly_valheim/ZSyncAnimation.cs
+++ C:/Users/donfr/valheim-shadows/src/client-1.0.7/assembly_valheim/ZSyncAnimation.cs
@@ -6,8 +6,6 @@
 		m_animator = GetComponentInChildren<Animator>();
 		m_animator.logWarnings = false;
 		m_nview.Register<string>("SetTrigger", RPC_SetTrigger);
-		ZDO zDO = m_nview.GetZDO();
-		bool flag = zDO?.IsOwner() ?? false;
 		m_boolHashes = new int[m_syncBools.Count];
 		m_boolDefaults = new bool[m_syncBools.Count];
 		for (int i = 0; i < m_syncBools.Count; i++)
@@ -29,12 +27,7 @@
 			m_intHashes[k] = GetHash(m_syncInts[k]);
 			m_intDefaults[k] = m_animator.GetInteger(m_intHashes[k]);
 		}
-		if (flag)
-		{
-			m_animSpeed = zDO.GetFloat(s_animSpeedID, 1f);
-			m_animator.speed = m_animSpeed;
-		}
-		if (zDO == null)
+		if (m_nview.GetZDO() == null)
 		{
 			base.enabled = false;
 		}
```

### ZSyncTransform

**`ZSyncTransform.OwnerSync`** - writes s_velHash only when the velocity changes from its cache, so our write survives

```diff
--- C:/Users/donfr/valheim-shadows/src/baseline-client/assembly_valheim/ZSyncTransform.cs
+++ C:/Users/donfr/valheim-shadows/src/client-1.0.7/assembly_valheim/ZSyncTransform.cs
@@ -48,6 +48,12 @@
 			{
 				Physics.SyncTransforms();
 			}
+			if ((bool)GetComponent<FloatingTerrain>())
+			{
+				Rigidbody body = FloatingTerrain.GetBody(base.gameObject);
+				body.linearVelocity = Vector3.zero;
+				body.transform.position = base.transform.position;
+			}
 			return;
 		}
 		if (m_syncPosition)
@@ -104,16 +110,12 @@
 		}
 		if (m_syncScale && base.transform.hasChanged)
 		{
-			if (Mathf.Approximately(base.transform.localScale.x, base.transform.localScale.y) && Mathf.Approximately(base.transform.localScale.x, base.transform.localScale.z))
-			{
-				zDO.RemoveVec3(ZDOVars.s_scaleHash);
-				zDO.Set(ZDOVars.s_scaleScalarHash, base.transform.localScale.x);
-			}
-			else
+			if (m_lastLocalScale.Equals(base.transform.localScale))
 			{
-				zDO.RemoveFloat(ZDOVars.s_scaleScalarHash);
-				zDO.Set(ZDOVars.s_scaleHash, base.transform.localScale);
+				return;
 			}
+			m_lastLocalScale = base.transform.localScale;
+			zDO.Set(ZDOVars.s_scaleHash, m_lastLocalScale);
 		}
 		if ((bool)m_body)
 		{
```

